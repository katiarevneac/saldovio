# Saldovio — CLAUDE.md

Operational guide for Claude Code sessions on this project. Full context, rationale, and open questions live in `Saldovio-Project-Brief.md` — read that first if this file is insufficient. That file predates the rename and refers to the product as "FinPilot" throughout; the current name is **Saldovio**. This file is the distilled, load-bearing subset: decisions already made, rules that must not be silently reversed, and the current state of work.

## What this project is

Saldovio (formerly named FinPilot during early planning): a personal finance SaaS, built incrementally as both a real, usable product and a learning vehicle (software development, architecture, databases, Git, Engineering Management, Product Ownership, Agile/Scrum with Jira/Confluence). The user is a beginner in web development with an engineering-management background in automotive software. Do not generate the whole app at once. Do not skip the teaching cycle for a shortcut.

## Non-negotiable decisions

- **Architecture:** three independently deployable components — Web (Next.js), Finance API (NestJS), Analytics Service (Python/FastAPI). Do not collapse this into a Next.js monolith and do not move financial logic into Next.js for convenience. If a change to this is proposed, it must be explained and discussed with the user first, never applied silently.
- Browser never talks to PostgreSQL directly. Finance API owns authorization and all writes/reads to the database.
- Analytics Service does not touch the database initially and does not block core transaction management if it's unavailable — an unavailable forecast must never render as zero.
- Money is stored as precise decimals (PostgreSQL `numeric`), never floats/JS `Number`, for monetary calculations. Precision/scale to be documented when the schema is designed.
- RON only for MVP. No bank integration, no AI/chatbot, no scoring 0–100, no monetization in MVP scope.

## Language rules

- Explanations and lessons in this conversation: Romanian.
- Code, commit messages, tickets, portfolio documentation: English.
- Caveman mode may be active in the session (compressed prose) — this does not change the Romanian/English split above, only the verbosity of explanations.

## Tech stack

**Accepted:**
- TypeScript for Web and Finance API.
- React + Next.js (frontend).
- NestJS (Finance API).
- Python + FastAPI (Analytics Service).
- PostgreSQL.
- Git + GitHub. Docker and GitHub Actions introduced gradually.

**Proposed, not yet locked in** — validate against current official docs via context7 before implementing, do not assume versions from prior conversation:
- Prisma for NestJS data access (after raw SQL exercises).
- Plain CSS before Tailwind; shadcn/ui later if it helps.
- Recharts for charts.
- Vitest (TypeScript logic) and Playwright (browser flows). Test runner for NestJS and for Python not yet chosen.
- Mature auth provider (Auth.js/Clerk were examples, not final picks).
- Hosting not chosen for any component.

**Environment on this machine** (verified 2026-09-08): Node v26.5.0, npm 11.17.0, Git 2.55.0. `gh` CLI not installed. Re-verify versions before relying on them if this note goes stale — do not assume it stays current.

## Mandatory tooling rule

**Always use context7 (`plugin:context7:context7`) to fetch current documentation before using any library, framework, or CLI feature** — Next.js, NestJS, FastAPI, PostgreSQL, Prisma, testing tools, etc. Do not rely on training-data memory for API syntax, config, or setup steps, even for well-known libraries. This applies to every implementation step, not just initial setup.

## Financial correctness rules (from brief §10–11)

1. A transfer between the user's own accounts is not income or expense at the aggregate level — total balance across accounts is unchanged. Model transfers as their own transaction type, not as income-on-B + expense-on-A.
2. A planned/recurring payment and the transaction that confirms it must not both be subtracted.
3. Repeated import of the same CSV file must be handled explicitly; never invent bank identifiers not present in the file.
4. Starting balance and its reference date must be defined so history is never double-counted.
5. Recurrence calendars, period boundaries, and nonexistent calendar days (e.g. day 31 in a 30-day month) need explicit rules and tests.
6. Indicators with a zero denominator or insufficient data return "unavailable/explained," never a fabricated value.
7. Every simulation/forecast result shows its assumptions, relevant inputs, calculation date, and formula version.

### Known limitation — backdated transactions (found 2026-09-09, Sprint 4 S2)

`accounts.current_balance` is a fixed snapshot as of `reference_date` (inclusive). A transaction with `occurred_on <= reference_date`, added *after the fact*, is **not** reflected in the displayed balance — it's assumed already baked into the stored `current_balance`. This is by design (rule 4 above — no double-counting), not a bug, but it means there is currently no way to correctly log a transaction that predates an account's `reference_date`.

Workaround once multi-account support exists (not yet built): a separate account (e.g. "Cash") can have its own earlier `reference_date`, since `reference_date` is per-account. Not a fix for the single-account case.

Not yet surfaced in the UI — a user can currently add a backdated transaction with no warning that it won't affect the balance. Revisit if this causes real confusion (it did once, during Sprint 4 S2 testing).

### Definition — "sold estimat" (agreed 2026-09-08, window revised 2026-09-15)

```
Sold estimat(azi → windowEnd) =
    Sold curent (suma tuturor conturilor utilizatorului, la data calculului)
  + Σ venituri recurente confirmate în intervalul [azi, windowEnd)
  − Σ cheltuieli recurente confirmate în intervalul [azi, windowEnd)

Transferuri între conturi proprii: excluse din venit/cheltuială (sold total neafectat).
```

Scope level: all of the user's accounts combined, not per-account.
Limitation (must be visible in UI, not hidden): forecast uses only confirmed recurring rules. One-off irregular past expenses are not statistically extrapolated.
Interval convention: `[azi, windowEnd)` — a recurrence landing exactly today is included; one landing exactly on `windowEnd` is not.

**`windowEnd` is now configurable (Epic 11 Story 1, 2026-09-15), revising the originally-agreed fixed `azi+30` window:** computed by `web/lib/forecast-window.ts`'s `computeWindowEnd(today, payday, horizonDays)` — if the user has set a `payday` (User.payday, 1-31, clamped to the month's last real day like `RecurringRule.dayOfMonth`), `windowEnd` is that day's next strictly-future occurrence; otherwise it falls back to `today + horizonDays` (`User.horizonDays`, default `30`). A user who has never touched Settings gets exactly the original `azi+30` behavior — the revision is additive, not a behavior change by default. `analytics-service/forecast.py` no longer owns any notion of "30 days" — it receives `window_end_date` as a required input (bounded to ≤366 days out, a defensive ceiling, not a product constant) and computes purely over whatever window it's given.

## Security rules (from brief §12)

- Authentication and authorization are distinct concepts, taught explicitly, not conflated.
- Server-side ownership checks on every data access — never trust a `userId` sent by the browser.
- Negative-path tests: user A must not be able to read/modify user B's data.
- Idempotency (or equivalent) for repeatable requests — no duplicate financial transactions from retries or double-clicks.
- Atomicity for multi-step writes that must succeed together.
- No secrets or unnecessary financial data in logs.
- Demo/fake data kept separate from real data. Real data only after relevant security/correctness/recovery checks exist.

## Minimal data model (derived so far, not final schema)

| Entity | Key fields | Why |
|---|---|---|
| User | id, email | ownership root |
| Account | id, user_id, name, current_balance, reference_date | sum of accounts = starting balance |
| Transaction | id, account_id, type (income/expense/transfer), amount, date, category | `transfer` as its own type enforces rule §1 above |
| RecurringRule | id, account_id, type, amount, frequency, day_of_month, active | forecast reads recurring rules, not history |

Schema is not finalized — this is the minimum implied by the rules agreed so far. Do not treat it as a migration-ready design.

## Teaching cycle (must follow, per brief §13)

For every stage: 1) user's problem + target outcome, 2) mini-lesson with terms explained from scratch, 3) alternatives + decision with reasoning, 4) a concrete exercise/contribution from the user, 5) small tracked implementation, 6) test + demo, 7) PO/EM reflection.

Rules:
- A feature isn't "learned" just because it works — the user must be able to explain the data path, possible errors, and how it was verified.
- Don't introduce many new tools in one lesson.
- Work step by step; don't dump multiple roadmap stages in one response unless asked.
- Decisions can be challenged and revised with evidence — don't present preferences as universal truth.

## Roadmap stage reference (brief §19, non-binding on dates)

0 Product Brief + backlog → 1 static page w/ fake data + repo → 2 first transaction saved Web+API+DB → 3 per-user auth → 4 correct dashboard → 5 Analytics Service + 30-day forecast → 6 simulator → 7 CSV import → 8 demo/CI/CD → 9 invited beta → later: credit models/monetization.

## Progress log

Update this section at the end of each session: what's confirmed, what was built, what's next. Keep it short — this is a pointer, not a transcript.

- **2026-09-08:** Read full brief. Confirmed architecture/objective. Lesson 1 done (browser/API/DB roles, why browser can't touch DB directly). "Sold estimat" definition agreed (see above). Environment checked: Node v26.5.0, npm 11.17.0, Git 2.55.0, VS Code, GitHub account exists, no `gh` CLI, no repo initialized yet. Data model above is derived, not finalized. Capacity: 20h/week, 1-week sprints. Initial backlog drafted (Epic: static fake-data dashboard page + repo, brief §19 stage 1). Sprint 1 goal proposed: repo on GitHub + static HTML/CSS/JS page with fake balance and transactions, no backend. **Product renamed FinPilot → Saldovio** — brief file renamed to `Saldovio-Project-Brief.md` with a rename note at the top; brief content otherwise left as originally written (still says "FinPilot" throughout, intentionally). Jira site exists: `saldovio.atlassian.net` — mid-setup, creating a Scrum project named "Saldovio". Local repo initialized: `git init`, initial commit `c35ea44` made by the user, default branch renamed `master` → `main`. Jira project **Saldovio** created (Scrum template), site `saldovio.atlassian.net`. Epic `Foundation — repository + fake-data dashboard page` created, linked to both stories: `SAL-2` (Git repository with clear README), `SAL-3` (Dashboard page with fake balance and transactions). Decided to skip separate Task-type issues for this sprint — scope too small to justify the process overhead. Sprint `SAL Sprint 1` started: 1 week, 2026-09-08 to 2026-09-15, goal "Repository on GitHub with README, plus a static HTML/CSS/JS page displaying fake account balance and transactions — no backend yet." SAL-2 done: README + .gitignore committed (`4f04274`), GitHub repo created (`katiarevneac/saldovio`, public), pushed. SAL-3 done: static dashboard built on `feature/SAL-3-static-dashboard` — `prototype/index.html`, `prototype/app.js`, `prototype/styles.css`. Amounts stored as integer minor units (bani), never floats — `formatAmount` divides by 100 only at display time via `Intl.NumberFormat('ro-RO', ...)`. Balance is computed with `.reduce()` over the fake transactions array, not hardcoded. PR #1 opened and merged into `main` (commit `ca5b624`), local branch deleted after merge. Sprint 1 (2026-09-08 to 2026-09-15) goal met with both stories done, ahead of the 1-week deadline. Sprint 1 closed early, retro written (`docs/retro/sprint-1.md` — written by Claude at the user's request, not the user; flagged as a process gap to revisit). **Sprint 2 started** (2 weeks, scope tripled vs. Sprint 1 on the user's call): Epic "Web + API + DB integration — first real transaction saved", broken into 3 sequential stories to avoid introducing too many new tools at once (brief §13) — S1 (PostgreSQL + transactions table), S2 (NestJS POST /transactions endpoint), S3 (dashboard form calls the API for real).

**S1 done:** found an existing Postgres.app 18.x install (not Homebrew) with the server already running — other local databases exist (`helpdesk`, `routewise_dev`) from unrelated projects, left untouched. Added Postgres.app's bin to `~/.zshrc` PATH. Created dedicated dev database `saldovio_dev`. Migration `db/migrations/0001_create_accounts_and_transactions.sql` — `accounts` and `transactions` tables, no `users` table yet (auth is roadmap stage 3). **Money in Postgres uses `NUMERIC(14,2)`, not integer minor units** — different convention from `prototype/app.js`'s integer-bani approach, and deliberately so: `numeric` is exact decimal natively, so Postgres doesn't need the JS workaround. Both solve the same float-precision problem, each with the tool suited to its layer. FK constraint (`transactions.account_id → accounts.id`) verified by hand: an insert with a nonexistent `account_id` was rejected by Postgres itself, not application code — concrete proof of brief §12 ("verificări server-side, integritate impusă, nu doar de aplicație"). Migration committed and pushed (`be5ad0d`).

No separate Task-type Jira issues were created for S1/S2/S3 (same simplification as Sprint 1) — work is tracked at the story level.

**S2 done:** scaffolded `finance-api/` (NestJS 12, native ESM — `"type": "module"` + `nodenext` module resolution, so relative imports need explicit `.js` extensions even in `.ts` source). Vitest and oxlint are the CLI's current defaults, not Jest/ESLint. Added `pg`, `class-validator`, `class-transformer`. `TransactionsModule` (controller + service + DTO) with `POST /transactions`, raw parameterized SQL (no ORM yet — matches the "SQL before Prisma" sequencing), global `ValidationPipe` in `main.ts`. DB connection (`src/database/pool.ts`) relies on `pg`'s built-in env-var/localhost defaults — no `.env` needed for local dev, same convention `psql` already used. **Found and fixed a real bug:** `pg` parses `DATE` columns into JS `Date` objects at local midnight, which serialize to UTC and can shift the displayed date by a day (`2026-09-10` → `"2026-09-09T21:00:00.000Z"`) — the stored value was always correct, only the API's JSON output was wrong. Fixed with `pg.types.setTypeParser(1082, val => val)` to keep DATE columns as raw strings; verified against node-postgres docs via context7 before applying. Endpoint tested via curl: valid POST → 201 + real row in `saldovio_dev`; invalid `type` → 400 from ValidationPipe before hitting application code.

**Process gap surfaced:** S1 and S2 were committed straight to `main`, breaking from the branch → PR → merge flow used for SAL-3 (brief §18) — not caught until after the fact, too late to undo without rewriting history. Decision: resume branch + PR for S3 onward.

**S3 done, on branch + PR as decided.** Added `GET /transactions` (needed for the page to show real data, not just accept writes) and `app.enableCors()` — browser fetch across ports is cross-origin, blocked by default. `prototype/app.js` rewritten: hardcoded fake array removed entirely, page now fetches real rows on load and re-fetches after every successful save (simpler and more honest than optimistic local updates — the page always reflects actual DB state). `toBani()` parses the API's decimal-string amounts (Postgres `numeric`) via string manipulation rather than `parseFloat`, to avoid reintroducing float error on the client. Served `prototype/` via `python3 -m http.server` for testing — `file://` origin is unreliable for `fetch`. PR merged (`48c8762`), branch deleted after merge.

**Sprint 2 complete — all 3 stories (S1, S2, S3) done, well inside the 2-week window.** First real end-to-end flow exists: browser form → NestJS API → PostgreSQL, with a validation layer (DTO + ValidationPipe) and a real bug found and fixed along the way (date timezone round-trip in `pg`'s DATE parsing). Sprint 2 retro written (`docs/retro/sprint-2.md`, same authorship caveat as Sprint 1 — user declined to write it herself again).

**Sprint 3 started** (2 weeks, same cadence as Sprint 2): Epic 3 "Next.js migration + authentication — per-user data isolation". Architecture decision: BFF (Backend-for-Frontend) pattern for auth — browser talks only to Next.js (session cookie, same-origin), Next.js calls Finance API server-to-server with a shared secret asserting the authenticated user, rather than NestJS independently verifying Auth.js's JWT. Chosen because Auth.js is Next.js-native; avoids manual JWT-verification plumbing in NestJS. Auth provider: Auth.js/NextAuth. Broken into 4 sequential stories: S1 (scaffold `web/`, migrate read-only dashboard), S2 (add-transaction form in Next.js), S3 (login/signup with Auth.js), S4 (Finance API requires auth + enforces per-user ownership).

**S1 done, on branch + PR.** Scaffolded `web/` — Next.js 16.3.4, React 19.2.8, TypeScript, App Router, no Tailwind. Read-only dashboard migrated from `prototype/` into an async Server Component (`web/app/page.tsx`) that fetches `GET /transactions` from the Finance API with `cache: "no-store"` (Next.js's current default — no caching unless opted in, verified via context7 and the locally bundled Next.js docs). Money logic ported into `web/lib/money.ts` (`toBani`/`formatAmount`, same string-parsing approach as `prototype/app.js` — no `parseFloat` on the API's decimal strings) and `web/lib/transactions.ts`. Styled via CSS Modules (`web/app/page.module.css`). Verified with `tsc --noEmit` and `eslint` (clean), dev server run on port 3001 (3000 taken by finance-api), confirmed against real API data and visually matched to `prototype/`'s behavior by the user. Committed as two commits (`3d2178e` scaffold, `0ddbdcf` migrate dashboard) on `feature/E3-S1-nextjs-dashboard`, PR #3 opened and merged (`b1a4924`), branch deleted after merge. `prototype/` is now superseded by `web/` and will be retired once `web/` reaches full feature parity — not yet done, kept for now as the S2 reference until the form is ported. **Next:** mark S1 Done in Jira, start S2 (add-transaction form in `web/`, same BFF-less direct-to-Finance-API pattern as S1's read, matching brief §12 negative-path/ownership rules once auth lands in S3-S4).

**S2 done, on branch + PR.** Added `web/components/TransactionForm.tsx` — Client Component (`"use client"`), controlled inputs for type/amount/date/category, sign derived from `type` (not typed by the user, same rule as `prototype/app.js`). `createTransaction()` added to `web/lib/transactions.ts`, POSTs to the Finance API with the same payload shape the DTO expects (`accountId` hardcoded to `1` — single-account MVP, no accounts endpoint yet). On success, calls `router.refresh()` (`next/navigation`) — confirmed via context7 that this re-fetches the current route's Server Component tree (re-running `getTransactions()`, which uses `cache: "no-store"`) while preserving Client Component/browser state, distinct from a full page reload. This is why the list/balance update after submit without any client-side optimistic state. Verified: `tsc --noEmit` and `eslint` clean, transaction added via the browser form, appeared in the list, balance updated. PR #4 merged (`1710faf`), branch deleted after merge.

Scale question raised by the user (10M-user hypothetical): confirmed the Server Component + `router.refresh()` pattern is not itself a scaling bottleneck (cost is O(1) per user action, not systemic) — real scale risks live in DB connection pooling, caching, and horizontal API scaling, none of which are MVP concerns yet. The actual near-term issue flagged is security, not scale: hardcoded `accountId`, no auth, no ownership checks — already the explicit target of S3-S4.

**S3 done, on branch + PR.** Migration `db/migrations/0002_create_users.sql` adds a `users` table (`email` UNIQUE, `password_hash`, `created_at`) — authentication identity only, no `user_id` FK on `accounts`/`transactions` yet (that's S4's ownership-enforcement job, deliberately kept separate). `finance-api`: `POST /users` (signup, `bcryptjs` hash, 10 rounds) and `POST /auth/login` (verify email+password; same "Invalid credentials" message whether the email doesn't exist or the password is wrong, so the endpoint doesn't leak which registered emails exist). Both tested via curl: signup 201, duplicate email 409 (caught via Postgres unique-violation error code `23505`), login 200 on success, 401 on wrong password and on unknown email.

`web`: Auth.js v5 (`next-auth@beta`), Credentials provider, `session: { strategy: "jwt" }` — required explicitly because Credentials doesn't support the `"database"` session strategy, confirmed via context7 (`UnsupportedStrategy` error). **Key architecture point:** `authorize()` runs server-side inside `web/`, but per the non-negotiable "Finance API owns all DB access" rule, it does not query Postgres directly — it calls Finance API's own `POST /auth/login` over HTTP, same as any other client would. `AUTH_SECRET` generated locally, stored in `web/.env.local` (gitignored, not committed). `/login` and `/signup` pages use plain `<form action={...}>` Server Actions calling `signIn`/`signOut` from `@/auth` — Auth.js's own documented v5 pattern, no client-side JS or `next-auth/react` needed. Dashboard (`page.tsx`) now shows the signed-in user's email + a logout form when a session exists. **Dashboard is intentionally NOT gated behind login yet** — with no `user_id` column on `accounts`/`transactions`, gating would be cosmetic, not real enforcement; that's explicitly S4.

Learning checks passed: user correctly explained JWT vs database sessions (self-contained signed cookie vs server-side session lookup) and correctly reasoned through why `strategy: "jwt"` is currently redundant (no adapter configured, so it's already the default) but guards against a future `UnsupportedStrategy` break if an OAuth adapter is added later.

**S4 done, on branch + PR — Sprint 3 complete (S1–S4 all done).** Migration `db/migrations/0003_add_user_id_to_accounts.sql`: `accounts.user_id` added nullable, existing dev account backfilled to `test@example.com`, then set `NOT NULL`. `finance-api`: signup (`UsersService.create`) now creates the user + a default "Cont curent" account in a single DB transaction (`BEGIN`/`COMMIT`/`ROLLBACK` via `pool.connect()`, first hands-on use of explicit transactions — brief §12 atomicity) — without this, a new user would have no account to transact against.

**Trust mechanism, decided over the static-secret alternative:** `InternalAuthGuard` (`finance-api/src/auth/internal-auth.guard.ts`) verifies a short-lived (30s) HS256 JWT signed by `web/` with a secret only the two services share (`INTERNAL_API_SECRET`, separate from Auth.js's `AUTH_SECRET`, gitignored in both `web/.env.local` and `finance-api/.env`). The user id lives inside the signed `sub` claim, not a separate spoofable header — chosen over a static-secret-plus-`X-User-Id`-header scheme specifically because a leaked static secret would grant permanent impersonation of any user, while a leaked JWT is only usable for its ~30s remaining lifetime. `GET /accounts/me` and `GET /transactions` scope to the caller's own data (join/filter on `user_id`); `POST /transactions` verifies the target `accountId` actually belongs to the caller before writing, 403 otherwise.

`web`: `lib/internal-auth.ts` signs the token server-side using `jose`, guarded by `import "server-only"` (build fails if ever imported into a Client Component — the secret must never reach the browser bundle). This forced `TransactionForm`'s write off a direct browser→Finance API fetch and onto a Server Action (`web/app/actions.ts`) — the browser can no longer reach Finance API at all; every call is now server-side and signed. `accountId` is no longer hardcoded — `web/lib/accounts.ts` fetches the caller's real account via `GET /accounts/me`.

Verified via a curl negative-path suite: no token → 401, malformed token → 401, forged signature (correctly-shaped JWT signed with the wrong secret) → 401, well-signed token for a nonexistent user id → 200 with an empty list (no error that would leak account existence), cross-user write attempt on another user's account → 403. Signup's atomicity confirmed directly in `psql`. Full browser flow confirmed by the user: a pre-existing user sees their existing data under the new ownership filtering, and a freshly signed-up user gets an account automatically and can add a transaction visible only in their own list.

Learning checks passed: user correctly reasoned that a stolen static shared-secret would allow permanent impersonation (no expiry, `X-User-Id` uncoupled from the secret), self-corrected after an initial answer that conflated the risk with browser-side credential storage (there is none — `server-only` prevents that by construction); also correctly reasoned that a stolen *valid* JWT is fully usable for its remaining ~30s window (not "safe by default"), with the real protection being the short, non-renewable exposure window rather than immunity.

**Sprint 3 complete: S1–S4 all done.** Epic 3 goal met — Next.js migration, Auth.js login/signup, and real per-user ownership enforcement on Finance API, replacing the fully public dev-mode API from Sprint 2. Sprint 3 retro written (`docs/retro/sprint-3.md`), same authorship caveat, now the third sprint in a row with this pattern. Sprint 3 closed, **Sprint 4 started** (1 week, 2026-09-09 → 2026-09-16): Epic "Correct dashboard — accurate balance calculation" (roadmap stage 4). Root cause: `accounts.current_balance`/`reference_date` existed in the schema since Sprint 2 but the dashboard ignored them entirely, just summing all transaction history — a real instance of brief §11 rule 4 (double-counting risk). Two stories: S1 (Finance API computes correct balance), S2 (dashboard displays it).

**S1 done, on branch + PR.** `AccountsService.findMine` now computes `balance = current_balance + SUM(transactions.amount WHERE occurred_on > reference_date)` server-side (Postgres `FILTER` clause), exposed on `GET /accounts/me`. Convention decided with the user: `reference_date` is inclusive in `current_balance` — only strictly-later transactions get added, avoiding double-counting a transaction dated exactly on it. Formula validated directly in `psql` against real data before writing TypeScript. PR #7 merged.

**S2 done.** `web/app/page.tsx` now displays `account.balance` (from `GET /accounts/me`) instead of `reduce()`-ing the full transaction list client-side. **Process note:** this story was initially committed directly to `main` without a branch — the exact mistake Sprint 2's retro flagged and Sprint 3 had avoided throughout. Caught before the commit landed (uncommitted changes moved onto a proper branch via `git checkout -b` before committing), not after — an improvement on how Sprint 2's version of this mistake was caught, but the underlying habit (branch first, always) still needs reinforcing rather than assumed.

**Real limitation surfaced during S2 testing** (see the new "Known limitation — backdated transactions" note under Financial correctness rules above): backdating a transaction to before `reference_date` silently has no effect on the displayed balance. Confirmed as correct-per-design (not a bug) with the user, documented rather than silently left implicit. This also surfaced a genuine backlog item: **multi-account support** (e.g. a separate "Cash" account with its own `reference_date`) is not built — schema already supports multiple accounts per user, but there's no `POST /accounts` endpoint, no account-creation UI, and no account-selection in the transaction form. Not in Sprint 4 scope; noted for a future sprint.

PR #8 merged. **Sprint 4 complete: S1+S2 both done**, dashboard balance now matches the financial model instead of a naive transaction sum.

**Process change (2026-09-09):** user gave direct feedback that end-of-story "explain in your own words" recall checks (brief §13's learning-verification step) aren't helping her — Jira/Scrum practice is the part that's actually landing. Dropped recall-quiz questions going forward; kept mini-lessons/technical explanations and Jira/PM guidance. Saved as a standing feedback memory outside this repo.

Sprint 4 closed. **Sprint 5 started** (1 week, 2026-09-09 → 2026-09-16): Epic "Multi-account support" — resolves the backdated-transaction limitation from Sprint 4 by letting each account carry its own `reference_date`. Scope decided via a brainstorming pass (2 clarifying questions, 2-3 approaches considered): dashboard shows aggregate total **and** a per-account breakdown; account management is create + select only, no edit/delete (YAGNI — deferred until a real need for it shows up). Two stories: S1 (Finance API `POST /accounts`), S2 (web: selector + creation page + dashboard breakdown).

**S1 done.** `AccountsService.create()` inserts a new account owned by the caller; `POST /accounts` reuses the existing `InternalAuthGuard` (no new auth code needed — same guard already covers every `/accounts` route). Verified via curl: create → 201, appears in `GET /accounts/me`; no token → 401. PR #9 merged.

**S2 done.** `getMyAccount()` → `getMyAccounts()` (full list, no longer just the first). `TransactionForm` now takes an `accounts` prop and a real `<select>` instead of an implicit first-account pick; `createTransactionAction` takes `accountId` from the form. New `/accounts/new` page + `createAccountAction`, mirroring the `/signup` Server Action pattern. Dashboard: total balance = sum of all accounts' `balance` fields (still integer-bani arithmetic, no floats); new "Accounts" section lists each account with its own balance. Verified end-to-end in the browser: created a "Cash" account with an earlier `reference_date`, added a transaction dated in that account's past — correctly reflected in both its own balance and the aggregate total, confirming the Sprint 4 limitation is resolved for accounts that opt into it. PR #10 merged.

**Sprint 5 complete: S1+S2 both done**, well inside the 1-week window. Found (not touched, out of scope): an untracked `design_handoff_saldovio_dashboard/` directory appeared in the repo root during S2 — a design-tool export, not created by this session's work; flagged to the user rather than silently added to or ignored by git.

Sprint 5 closed. **Sprint 6 started** (2 weeks, 2026-09-09 → 2026-09-23): Epic "30-day balance forecast — Analytics Service", roadmap stage 5. User chose one combined sprint over splitting schema-work from the Analytics Service itself (considered and explicitly declined the 2-sprint split this session recommended). Four stories: S1 (`RecurringRule` schema + Finance API CRUD), S2 (recurring-rule creation UI), S3 (Analytics Service, FastAPI, `/forecast`), S4 (dashboard forecast display with graceful fallback).

**Data-flow decision (brainstormed with the user):** `web/` collects balance + recurring rules from Finance API and sends them to Analytics Service as input — Analytics Service does not call Finance API itself. Chosen over the alternative (Analytics Service calling Finance API directly) specifically to avoid spreading `INTERNAL_API_SECRET` to a third service and to keep Analytics Service a pure, dependency-free calculator matching the standing rule that its unavailability must never affect core transaction management.

**Schema decision:** `recurring_rules.frequency` is `monthly`-only for now (CHECK-enforced) — weekly/daily deferred until a real need surfaces. Nonexistent-day handling (`day_of_month` 31 in a 30-day month) clamps to the month's last day — decided with the user, will be implemented in S3's forecast computation, not at the storage layer.

**S1 done.** `db/migrations/0004_create_recurring_rules.sql` — table only stores the rule; no forecast logic lives here. `POST`/`GET /recurring-rules` mirror the transactions pattern exactly (ownership check on create, `JOIN accounts` scoping on read), reusing `InternalAuthGuard` unchanged. Verified via curl: create → 201, invalid `dayOfMonth` (32) → 400 from `ValidationPipe` before reaching application code, no token → 401.

**Process change (2026-09-09):** user asked Claude to create and merge PRs directly via `gh` CLI going forward, instead of handing off title/description for her to action on GitHub. Installed `gh` via Homebrew, she ran `gh auth login` once. PR #11 (S1) was the first PR created and merged this way — same Why/What/How-verified description convention kept, just automated who clicks merge. Saved as a standing feedback memory outside this repo.

**S2 done.** `web/lib/recurring-rules.ts` (`getMyRecurringRules`), `createRecurringRuleAction` (mirrors `createAccountAction`), `/recurring-rules/new` page, dashboard "Recurring rules" section. **Found and fixed while wiring display:** `CreateRecurringRuleDto` didn't enforce `amount > 0` — added `@IsPositive()`. Recurring rules store amount as a positive magnitude, unlike transactions' signed amount — the forecast formula adds income and subtracts expense explicitly by `type`, so sign belongs in the formula (S3), not the stored value. Verified in browser: rule created, appears on dashboard with correct sign applied for display. PR #12 merged.

**S3 done — third architecture component built for the first time.** `analytics-service/` scaffolded (Python 3.14, FastAPI, venv + `requirements.txt`). `forecast.py` is a pure calculator — no DB, no outbound calls, matching the data-flow decision above. Money uses `Decimal` throughout; confirmed via context7 that Pydantic v2 always serializes `Decimal` as a JSON string (not a number), so no float round-trip risk on the `web/` side. Wire format is camelCase (`alias_generator=to_camel`) to match the rest of the stack's DTO convention. `day_of_month=31` clamps to the month's last real day. Response always carries `formula_version` + `assumptions` + both boundary dates (brief §11 rule 7).

Test runner for Python: **pytest** (no real alternative considered — Python's de facto standard, unlike the earlier NestJS/Vitest choice which had genuine options). 9 tests, including the two trickiest edges: an occurrence landing exactly on the window's exclusive end boundary (tested directly against the boundary helper, since real calendar dates rarely land exactly on a 30-day mark — two of the first-draft tests had wrong expectations here, not the code, caught by running them and reasoning through the failure rather than assuming the test was right), and a monthly rule occurring **twice** in one window near a month boundary (Jan 31 + 30 days = Mar 2, so both Feb 1 and Mar 1 fall inside). All 9 pass. Verified live via `TestClient` and a running `uvicorn` server + curl.

**Process note:** started this story directly on `main` again before catching it — same mistake as Sprint 4's S2, caught the same way (moved uncommitted work onto a branch via `git checkout -b` before committing, not after). Second recurrence of this exact slip within two sprints; worth treating as a checklist item to run through explicitly at the start of every story, not just remembered.

PR #13 merged (via `gh` CLI, the new workflow).

**S4 done — Sprint 6 complete, all 4 stories shipped.** `web/lib/analytics.ts` (`getForecast`, no internal-auth token — Analytics Service has no ownership concept), `baniToDecimalString()` added to `lib/money.ts` (inverse of `toBani`, needed to send the aggregate balance back out as a Decimal-compatible string). Dashboard's forecast fetch is wrapped in its own `try/catch`, isolated from the rest of the page's data — verified for real, not just by code inspection: killed the running Analytics Service process and reloaded the dashboard in the browser. Balance, accounts, and transactions all rendered correctly; the forecast section showed an explicit "Forecast unavailable" message instead of crashing the page or silently showing 0. Restarted the service, forecast returned correctly. This is the first story this sprint verified with a real fault-injection test rather than just the happy path. PR #14 merged.

**Sprint 6 complete: S1–S4 all done.** Third architecture component (Analytics Service) built, integrated, and proven resilient to its own downtime — the standing "unavailable forecast must never render as zero" rule is no longer just written down, it's been tested.

**Next:** mark S3+S4 Done in Jira, close Sprint 6, decide Sprint 7 scope.

Sprint 6 closed. A high-fidelity UI design handoff arrived (`design_handoff_saldovio_dashboard/`, untracked, not committed — design-tool export, kept as reference, out of `web/`'s build): dark/lime theme across 6 screens (Overview, Transactions, Accounts, Forecast, Simulator "Can I afford it?", Settings), two of which are new features not yet built (Simulator = roadmap stage 6, CSV import = roadmap stage 7). Too large for one spec (brief §13), decomposed via `superpowers:brainstorming` into 5 sequential sub-projects, each its own spec→plan→implementation cycle: **1** technical foundation (Zod, Prisma, two-layer route protection, Vitest/jsdom/RTL — no UI), **2** visual reskin (dark/lime theme on existing screens, no new features), **3** Simulator, **4** dedicated Forecast screen (3 chart modes), **5** Settings + CSV import. User requested continued Jira tracking (epics/sprints/stories, Claude-guided as always) and agent-driven implementation via Superpowers (`subagent-driven-development`).

**Sub-project 1 spec written and merged** (`docs/superpowers/specs/2026-09-09-technical-foundation-design.md`, PR #15): full Prisma migration (not partial — all 5 finance-api services), Zod in both layers (`nestjs-zod` replacing `class-validator` in finance-api — not yet done, that's S2; Zod schemas in `web` for form validation — not yet done), two-layer route protection (Next.js `proxy.ts` — renamed from `middleware.ts` in Next.js 16, now runs on Node.js runtime not edge — plus existing `auth()` server-side checks; dashboard becomes gated, currently isn't), Vitest+jsdom+RTL setup in `web` scoped to what changes.

**Sprint 7 started**, Epic 7 "Technical foundation": S1 (Prisma migration), S2 (Zod), S3 (route protection), S4 (Vitest/jsdom/RTL setup) — sequential, 2-week sprint.

**S1 done — Prisma migration, executed via `subagent-driven-development` (first time this project has used multi-agent orchestration for implementation, not just planning).** Plan: `docs/superpowers/plans/2026-09-09-prisma-migration.md`, 8 tasks, one fresh implementer subagent + one reviewer subagent per task, fix loop where needed. All 5 services (`users`, `accounts`, `transactions`, `recurring-rules`, `auth`) migrated off raw `pg` onto Prisma, pinned to `7.10.0` — deliberately not the npm `latest` tag, which currently points at an unstable `8.0.0-rc`; a financial-data ORM has no business on a release candidate. Schema introspected from the live `saldovio_dev` DB via `prisma db pull`, then baselined against the 4 hand-written SQL migrations in `db/migrations/` (kept as historical record, `prisma migrate` owns schema changes from here on). Shared `PrismaService`/`PrismaModule` (`@Global()`) plus `finance-api/src/common/serialization.ts` (`toDecimalString`, `toDateOnlyString`, `fromDateOnlyString`, `todayDateOnly`) — Prisma returns `NUMERIC` columns as `Decimal.js` objects and `DATE` columns need the same UTC-anchoring discipline already applied once at the raw `pg` layer (`database/pool.ts`, now deleted) and once at Analytics Service's Pydantic layer; this is the third independent enforcement of "money is never a float, dates never shift timezone," each layer re-verified with its own test rather than assumed from the pattern holding elsewhere. Atomic signup (user + default account) moved from manual `BEGIN`/`COMMIT`/`ROLLBACK` to `prisma.$transaction`, with a rollback test added in review (forces a real Postgres FK violation mid-transaction, asserts the first write didn't survive — not just structural trust that `$transaction` works). `findMine()`'s balance calculation kept as `$queryRaw` — Prisma's query builder can't express the FILTER-clause conditional aggregate the "sold estimat" formula needs, and the raw SQL was already validated in Sprint 4.

**Two per-task findings escalated to the user mid-build** (per `subagent-driven-development`'s rule: plan-mandated findings are a human decision, not the agent's to silently override): a missing `@db.Timestamptz(6)` on `User.createdAt` in the schema I'd written (fixed), and a missing rollback test for the atomic signup transaction (added). Both fixed same-session.

**Final whole-branch review** (dispatched on the most capable model, after all 8 tasks individually passed) caught 3 cross-task issues no single task's reviewer could see: the Prisma-generated baseline silently drops 4 CHECK constraints Prisma's schema language can't express (`transactions.type`, `recurring_rules.type`/`frequency`/`day_of_month`) — fixed by hand-appending the `ALTER TABLE ... ADD CONSTRAINT` statements to the baseline migration file, no live-DB change; no `postinstall: prisma generate` meant a fresh clone couldn't build (fixed, plus added `finance-api/.env.example` since `DATABASE_URL` is now a hard runtime requirement with zero prior documentation); and a date-parsing helper that could 500 on a technically-DTO-valid full ISO timestamp instead of a clean 400 (fixed, DTOs tightened to `@Matches` on `YYYY-MM-DD`). The fix for that last one introduced its own narrower gap — a re-review caught that the tightened regex was format-only, so calendar-invalid dates like `"2026-13-45"` slipped through where the old `@IsDateString()` used to catch them — surfaced to the user (not reachable from `web/`, since `<input type="date">` never emits invalid dates, but a real gap via direct API calls), user chose to fix it immediately rather than defer: `fromDateOnlyString` now verifies the constructed `Date`'s components round-trip against the parsed input, catching both outright `Invalid Date` and JS's silent month/day rollover (`"2026-02-30"` → `"2026-03-02"`) in one check. Noted, not fixed (legitimate future scope, not this epic's): a plain thrown `Error` from that helper still surfaces as an unhandled 500 rather than a 400, since no global exception filter exists anywhere in `finance-api` — would need new infrastructure, correctly left as a follow-up rather than scope-creeping this migration.

Dependency hygiene: `prisma` moved to `devDependencies` (was pulling Prisma Studio's entire UI stack — `@visx/*`, `d3-*`, `@radix-ui/*`, an embedded WASM Postgres — into the production install), unused `pg`/`@types/pg` removed (still present transitively via `@prisma/adapter-pg`).

No API contract or response-shape changes anywhere — every endpoint returns byte-identical JSON to before, `web/` is untouched. 26/26 tests pass (real `saldovio_dev`, no mocking anywhere in this branch, consistent with how every previous data-layer change in this project has been verified). PR #16 merged (`1d97eb4`), branch deleted.

**Next:** S2 (Zod — `nestjs-zod` in finance-api, form-validation schemas in `web`).

**S2 done — Zod, executed via `subagent-driven-development`.** Plan: `docs/superpowers/plans/2026-09-09-zod-validation.md`, 9 tasks. All 5 finance-api DTOs (`users`, `accounts`, `transactions`, `recurring-rules`, `auth/login`) migrated from `class-validator`/`class-transformer` to `nestjs-zod` (`createZodDto` + `ZodValidationPipe`), and all 4 `web` forms (signup, login, add-account, add-transaction) got matching client-side Zod schemas under `web/lib/schemas/`, sharing one error-parsing helper (`web/lib/api-errors.ts`) so server-side rejections and client-side validation failures render through the same path instead of two. Before Task 2 could even smoke-test, an out-of-band bug carried over from Epic 7 S1 surfaced: `tsconfig.build.json`'s `rootDir: "./src"` conflicted with the Prisma client generated as a sibling of `src/`, breaking `npm run start:dev` compilation entirely — fixed by moving the generated client to `src/generated/prisma` and shortening 11 import paths, unrelated to Zod itself but blocking every subsequent task's manual verification until resolved.

The final whole-branch review surfaced two undocumented behavior deltas from the class-validator → Zod migration, neither a bug, both worth recording since they silently tightened validation: (1) `@IsOptional()` accepted both `null` and `undefined`; Zod's `.optional()` only accepts `undefined`, so `POST /transactions`/`POST /recurring-rules` with an explicit `"category": null` now returns 400 where it used to return 201 — not reachable from `web/`, since every call site sends `category || undefined`, never `null`. (2) the old `ValidationPipe` had no `whitelist` set and silently forwarded unknown JSON keys into the DTO object (unused, but present); Zod's `z.object()` strips unknown keys by default — also silent, also harmless in practice. Also fixed per that review: `.npmrc`'s bare `legacy-peer-deps=true` now has a comment explaining it's covering `nestjs-zod@5.5.0`'s peer range not yet including NestJS 12, and an `oxlint` `no-unused-vars` warning in `create-transaction.dto.spec.ts` (destructured `category` binding renamed to `_category`).

**S3 done — Sprint 7 complete (S1–S4 all done), executed via `subagent-driven-development`.** Plan: `docs/superpowers/plans/2026-09-10-route-protection.md`, 3 tasks. Two independent layers: **layer 1** (`web/proxy.ts`, Next.js 16's rename of `middleware.ts`, plus an `authorized` callback in `web/auth.ts` backed by a pure `isPathAuthorized` function) — fast, route-level, UX only, not the real security boundary. **Layer 2** (explicit `auth()` + `redirect("/login")` inside each Server Component, before any data fetch) — the real authorization boundary, holds even if layer 1 is misconfigured. `web/` also got its first test infrastructure this task: Vitest + jsdom + `@testing-library/react`/`jest-dom`.

**Final whole-branch review (most capable model) caught a real spec-vs-plan gap:** the plan (written this session) had scoped layer 2 to the dashboard only; the original design spec (`docs/superpowers/specs/2026-09-09-technical-foundation-design.md`) requires it on *every* Server Component. Escalated to the user as a plan-conflict decision (not silently resolved) — user chose to extend layer 2 to `/accounts/new` and `/recurring-rules/new` immediately rather than defer. Without the fix, `/recurring-rules/new` 500'd with the raw text "Not authenticated" leaked into the response body for an unauthenticated request — a real, if narrow, information-disclosure gap, not just a UX one. Same review also caught `web/`'s `npm install` silently broken (`@types/node` pinned to `^20`, but `vitest@5.0.0` peer-requires `^22`/`^24`, papered over by a stale lockfile) — fixed by bumping to `^24`, both because it's the honest fix and because this machine already runs Node v26; and that `proxy.ts` had zero test coverage of its own export/matcher, meaning a renamed export (`proxy` → `middleware`, an easy slip right after the Next 15→16 rename) would have silently disabled layer 1 with a fully green suite — added `web/proxy.spec.ts`. All fixed in one fix wave + one scoped re-review, verified clean.

**Verified layer 2 is real, not just written down:** in an isolated copy, the final reviewer deleted `proxy.ts` entirely (layer 1 gone) and confirmed the dashboard still redirected an unauthenticated request — 307 to `/login`, no data fetch attempted. Layer 2 holds on its own.

**Known follow-up, not blocking:** no Postgres/Finance API was reachable in the execution environment used for this task, so the full logged-in dashboard render and a live (not code-inspection) check that `/accounts/new`/`/recurring-rules/new` redirect for a real unauthenticated browser request still need a human pass. PR #18 merged into `main` (`399aa15`).

**Sprint 7 complete: S1–S4 all done** (Prisma migration, Zod validation, route protection, plus S4 Vitest/jsdom/RTL setup folded into S3's test-infrastructure work rather than a separate story — the two were inseparable in practice once layer-1/layer-2 testing needed a working harness first). Sub-project 1 of 5 (technical foundation) for the dark/lime UI rework is done.

**Sprint 8 scoped via brainstorming — bigger than originally planned.** During design, the user explicitly chose to merge former sub-projects 2 (visual reskin), 3 (Simulator), and 4 (dedicated Forecast screen) into one Epic 8, after being shown the scope tradeoff (roughly 3x any prior sprint, a deviation from brief §13's "one roadmap stage at a time" guidance) — recorded as a deliberate decision, not an oversight. Spec: `docs/superpowers/specs/2026-09-10-dark-lime-ui-design.md`. Key decisions: routes split (`/`, `/transactions`, `/accounts`, sidebar with Forecast/Simulator/Settings disabled until built), add-transaction becomes a modal, recurring rules stay on Overview (not in the original design), Overview gets the real forecast chart and simulator card (not stand-ins — this is what pulled sub-projects 3/4 in), `/forecast` and `/simulator` built as full dedicated routes, Recharts adopted for the Line chart mode (locks in a previously-"proposed" stack choice), Analytics Service's `/forecast` extended with a `dailyBalances` series (discovered mid-brainstorm — the existing endpoint only returned the day-30 number, not a trajectory, so the chart needs a real backend extension, not just UI work), simulator math is an independent 4th client-side reimplementation of the recurrence logic (not derived from the fetched series — deliberate, matches this project's pattern of re-verifying money/date logic at every layer), verdict thresholds are a provisional percentage-of-balance rule (10%) pending real essential-spend data from Settings (sub-project 5, still out of scope). 12-story epic breakdown in the spec.

**Story 1 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-10-dark-lime-design-tokens.md`, 1 task. Full dark/lime CSS custom-property token set (colors, radii, spacing) in `web/app/globals.css`, replacing the old light/dark starter theme with a single fixed dark theme (no toggle — the design has one theme only). Archivo font wired in via `next/font/google` in `web/app/layout.tsx`, replacing Geist — and fixing a real latent bug in the process: Geist was loaded but `globals.css` never actually referenced `var(--font-geist-sans)` anywhere, so the app was silently falling back to plain Arial the whole time. Two shared global CSS utility classes added (`.card`, `.tabularNums`) for later stories to consume via `composes: ... from global;`.

Task review caught one Important finding: the implementer added an unused `params` destructuring to `RootLayout`'s signature, justified by an incorrect claim that Next.js 16's `LayoutProps<"/">` type required it — reviewer disproved this empirically (`tsc --noEmit` stays clean without it). Fixed in one round, verified by a scoped re-review. Final whole-branch review (most capable model) verified the implementation byte-for-byte against the plan's exact code (no scope creep, no extra tokens), confirmed via a real `npm run build` that the font and utility classes actually land in the emitted CSS (not just asserted by the necessarily-mocked test), and flagged one real but non-code judgment call: merging Story 1 alone leaves `main` in a transiently low-contrast state on pages Story 3 hasn't reskinned yet (hardcoded light-theme grays/reds in `page.module.css`, `login`/`signup`, `TransactionForm` now render against the new near-black background — most notably login/signup error messages). Escalated to the user rather than silently decided; user chose to merge now (matches the plan's own stated expectation and this project's per-story merge pattern) rather than stack Stories 2-3 first. Merged to `main` locally (`65e991d`), worktree and branch cleaned up.

**Story 2 done, executed via `subagent-driven-development`.** Plan: `docs/superpowers/plans/2026-09-10-sidebar-route-split.md`, 3 tasks. `web/components/Sidebar.tsx` — 6 nav items, `usePathname`-based `aria-current="page"` for the active route (not a CSS-module class, since those hash under Vite/Vitest and would make assertions fragile), and Forecast/Simulator/Settings rendered as plain non-interactive `<li aria-disabled="true">` rather than a styled-disabled `Link` (which would still be clickable/focusable). A new `web/app/(dashboard)/` route group holds all three pages behind a shared `layout.tsx` — chosen over putting the sidebar in the root layout (the spec's literal suggestion) specifically because the root layout also wraps `/login`/`/signup`, which must never show a sidebar; route groups are stripped from the URL, so `/` stays `/`. The transactions table and account list moved out of Overview into their own pages verbatim — same markup, same money handling, no visual reskin (that's Stories 3/5/6).

**Task reviews:** Tasks 1 and 3 were clean on first pass. Task 2 was clean too, with one Minor noted (the plan's own exact-replacement code for `page.tsx` incidentally dropped a pre-existing explanatory comment about Analytics Service fault isolation — a plan-text artifact, not implementer judgment, left for Story 3 to restore when it rewrites that file anyway).

**Final whole-branch review (most capable model) caught 2 Important cross-task regressions no single task's reviewer could see:** (1) nested `<main>` landmarks — Task 1's `layout.tsx` added a `<main>` wrapper, but Overview's own `page.tsx` (moved unchanged, then partially rewritten by Tasks 2-3) still had its own `<main>` inside that, producing invalid nested-landmark HTML; (2) log out became unreachable from `/transactions` and `/accounts` — the header (title, session email, sign-out form) lived only in Overview's `page.tsx`, so splitting the page into three routes silently removed the only way to sign out from two of them, a real functional regression the split itself introduced. Both fixed in one wave: the header moved into `(dashboard)/layout.tsx` (now `async`, calling `auth()` itself for the email display — a second, deliberate `auth()` call alongside each page's own unchanged layer-2 `auth()`+`redirect` guard, not a replacement for it; Next.js request memoization makes the extra call cheap). Verified via a dedicated `layout.spec.tsx` (`getAllByRole("main")` has length 1) plus a live build+server check that `/transactions` and `/accounts` still redirect unauthenticated requests and `/login` still renders no sidebar. Scoped re-review confirmed both fixes clean, no new breakage.

**5 Minor findings deferred to the ledger, not fixed now:** hardcoded pixel spacing in `Sidebar.module.css` (no spacing-token scale exists yet); `aria-disabled` on a `role="listitem"` element is not AT-supported per `jsx-a11y/role-supports-aria-props` (the disabled nav items are structurally correct — not clickable — but not announced as unavailable to screen readers; a real accessibility gap, cheapest fix is a visually-hidden "(coming soon)" span, left for a future pass since the plan mandated this exact markup); the two new pages render unpadded against the sidebar until Story 3 restyles `.main` (by design, per this story's plan); `/accounts/new` and `/recurring-rules/new` sit outside the `(dashboard)` route group, so the sidebar disappears during those flows (not anticipated when Story 1/2's scope was set — worth folding into Story 6's account-creation work); `Sidebar`'s active-route match is exact-equality only, so `/accounts/new` highlights nothing (same fix as the previous item, same story).

24/24 tests pass (`web`'s full suite). Merged to `main` locally (`08d3daa`, fast-forward), worktree and branch cleaned up.

**Next:** decide Story 3 scope (Overview reskin — KPI cards, forecast chart embed, simulator card embed) or address one of the deferred Minor items first (sidebar missing on `/accounts/new`/`/recurring-rules/new` is the most user-visible).

**Jira: Sprint 8 scoped for Stories 1-7** (dark/lime reskin complete — Overview/Transactions/Accounts/Login/Signup + add-transaction modal), after being shown a 3-way grouping option for the remaining 10 stories and choosing the one that respects the original (pre-merge) sub-project boundaries. Sprint 9 (Stories 8-10, Forecast) and Sprint 10 (Stories 11-12, Simulator) planned as separate future sprints, not yet started.

**Story 3 scope conflict found before planning, resolved with the user before writing the plan:** the spec's own Story 3 architecture calls for Overview's forecast chart and simulator card to be real, working embeds — but those depend on `ForecastChart` (Story 9) and `simulator.ts` (Story 11), both in Sprint 9/10, not Sprint 8. Presented 3 options (empty placeholder now / reorder the epic to pull Stories 8/9/11 forward / ship a data stub); user chose the placeholder — real reskin for KPI cards/recurring-rules/accounts/transactions, and a genuinely non-interactive "coming soon" card for the simulator section (dashed border, muted text, no fake numbers), consistent with the project's standing "no fabricated values" rule and the same disabled-item treatment already used for the Sidebar's not-yet-built nav entries.

**Story 3 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-10-overview-reskin.md`, 3 tasks. Task 1: `formatAmountValue` (`web/lib/money.ts`, a narrower sibling to the existing `formatAmount` — plain locale-formatted number with no "RON" symbol, for KPI cards that render the currency label and the number as separate typographic elements) + `KpiCard` component (`web/components/KpiCard.tsx`). Task 2: `computeMonthlyTotals`/`currentYearMonth` (`web/lib/overview-metrics.ts`) — a pure function deriving this-month income/expense/surplus from the already-fetched transaction list (no new Finance API endpoint), excluding `type === "transfer"` per brief §11 rule 1. Task 3: Overview page rewrite — 4 KPI cards (total balance, income/expenses/surplus this month), recurring-rules/accounts/transactions condensed cards (capped at 3/∞/5 with "View all" links), the existing forecast summary restyled into a card (same data/logic, no chart yet), and the simulator placeholder card. `(dashboard)/layout.module.css`'s `.main` also got the outer padding Story 2 explicitly deferred to this story — a shared-layout fix that benefits `/transactions` and `/accounts` too, even though their own internal reskin is still Stories 5/6.

**Task 1 fix round:** the plan's own Global Constraints listed `--space-card-padding` as a token `KpiCard` must reuse, but the plan's own code for `KpiCard.module.css` hardcoded `padding: 18px 20px` instead — a plan self-inconsistency, not an implementer deviation (confirmed: `--space-card-padding` is `20px`, matching the hardcoded horizontal value exactly). Resolved without escalating to the user, since the fix (`padding: 18px var(--space-card-padding)`) produces byte-identical rendered output while satisfying the constraint literally — not a real policy conflict, just a token substitution with zero visual change.

**Task 3 implementer hit a session-limit interruption mid-task** (first time this project has hit this) and was resumed via the same agent (full transcript intact) rather than restarted fresh — completed cleanly after resuming. Before being cut off, it had already correctly diagnosed a real bug in the plan's own test code: `page.spec.tsx`'s "condensed accounts" test asserted `getByText("A1")`, but account names also appear as `<option>` text inside `TransactionForm`'s account `<select>` (which correctly receives the full account list, not the condensed 3-item slice) — an unscoped query matches two elements. Fixed by scoping the test query with `within()`, not by changing any production markup. Also found and fixed a second latent test-infrastructure gap: `TransactionForm` calls `useRouter()` unconditionally at render time, but no prior version of `page.spec.tsx` had ever actually called RTL's `render()` on the full page (only checked mock-call assertions) — so the missing mock was never exercised until this story's tests started doing real DOM renders. Both fixes independently verified by the task reviewer as genuine test-only corrections, not production workarounds.

**Final whole-branch review (most capable model) caught 2 Important findings, both inherited defects in shared layout code that this story surfaced rather than created:** (1) `(dashboard)/layout.module.css`'s `.sessionEmail` still had a light-theme `color: #555` leftover, ~2.2:1 contrast against the dark background — swapped for `var(--color-text-secondary)` (~9:1). (2) the shared header's `<h1>Saldovio</h1>` brand plus Overview's own new hero `<h1>` meant two `<h1>`s per dashboard route, breaking the screen-reader heading outline — the brand demoted to `<p className={styles.brand}>` with CSS preserving its prior visual weight, and the two now-stale `getByRole("heading", {name:"Saldovio"})` test assertions updated to `getByText`. Both fixed in one wave, re-review clean. The review also independently verified two things no single task's reviewer could see: that the CSS Modules `composes: card from global` cascade order actually wins in the *built* output (checked the emitted CSS and `<link>` order directly, not just asserted from the pattern holding before), and that the shared `.main` padding change doesn't double up with anything on the still-unstyled `/transactions`/`/accounts` pages.

**9 Minor findings deferred to the ledger, not fixed now:** hero subtitle text drifted one clause from the design handoff's exact copy (plan-text issue, not implementation); the design's "planned payments not yet deducted" caveat isn't surfaced on the Total balance card (brief §11 rule 2 territory, worth adding next time that card's copy is touched); the Monthly surplus KPI always renders in the lime "good" accent color even when negative (plan-mandated, a one-line `variant={monthly.surplusBani < 0 ? "expense" : "accent"}` fix later); a dead `: null` fallback branch in the forecast section (unreachable — `getForecast` only ever throws or resolves, carried over from the pre-story version); `.tabularNums` in `page.module.css` does more than its name says (composes the global utility and also forces `font-weight: 700`); duplicated hardcoded type sizes across `KpiCard.module.css` and `page.module.css` — the spec's Story 1 token list included a type scale that Story 1 never actually shipped, so every story since has hand-rolled px values, worth fixing before Story 5; the recurring-rules card renders inactive rules identically to active ones (pre-existing since Sprint 6, noticed because the card was rebuilt here); a few page-level test gaps (recurring-rules card, the three empty states, page-level transfer-exclusion — the last one is unit-covered in `overview-metrics.spec.ts` already, which is reasonable); and two brittle anchored-regex text queries in `page.spec.tsx` that the `within()` pattern used elsewhere in the same file would make sturdier.

**Recommendation surfaced, not yet a scheduled story:** no story in the 12-story Epic 8 breakdown owns `(dashboard)/layout.tsx`'s shared header/topbar — this is how the `.sessionEmail` contrast bug and the duplicate-`<h1>` both slipped through Story 1/2 unnoticed. Worth assigning explicitly before more stories build on top of it.

38/38 tests pass (`web`'s full suite, no regressions). Merged to `main` locally (`c3e2d77`, fast-forward from `3eedf46`), worktree and branch cleaned up — a stale `git worktree` registration (directory already gone from disk, but still listed) needed `git worktree prune`/`git worktree remove --force` to clear, unlike the clean removals in Stories 1-2.

**Next:** mark Story 3 Done in Jira. Decide Story 4 scope (add-transaction modal) or address the layout-header recommendation above first.

**Story 4 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-11-add-transaction-modal.md`, 2 tasks. Task 1: `web/components/Modal.tsx` — a generic, domain-agnostic dialog primitive on the native `<dialog>` element (no portal library, no new dependency), controlled via `open`/`onClose`/`title`/`children` props; content unmounts when closed (form state can't leak between opens). Chose `showModal()` over a hand-rolled overlay specifically for the free behavior it buys — focus containment, focus restoration to the trigger, page inertness, Escape handling, top-layer stacking — none of which had to be hand-built. **Found mid-task:** the installed jsdom (30.0.1) doesn't implement `HTMLDialogElement.showModal`/`close` at all (bare `HTMLElementImpl` subclass, confirmed by reading jsdom's source directly) — added a conditional polyfill to `web/vitest.setup.ts` that self-disables once jsdom ships a real implementation. Task 2: `web/components/AddTransactionModal.tsx` (new) composes `Modal` with the existing `TransactionForm`, owns open/closed state, renders the "+ Add transaction" trigger. `TransactionForm.tsx` got one additive `onSuccess?: () => void` prop, called *after* `router.refresh()` inside the existing success path — verified in review that a failed submit leaves the modal open with the error still visible, not silently discarded. Server Action wiring, payload, and error handling otherwise byte-identical, matching the spec's explicit "not re-tested" testing scope.

**Final whole-branch review caught 2 Important findings, both pre-existing gaps this story made visible rather than introduced:** (1) `TransactionForm.module.css` had never been touched by any prior Epic 8 story — moving the form onto the dark modal surface exposed a hardcoded `#b3261e` error color at ~2.8:1 contrast (same defect class Story 3 already fixed once on `.sessionEmail`), an unstyled submit button sitting next to the new lime pill trigger, and a `max-width: 320px` leftover from the old full-width card layout leaving ~60px of dead gutter in the 420px dialog. (2) `Modal`'s `<dialog>` had no `aria-labelledby` linking it to its own title — flagged as worth fixing now specifically because this is a new shared primitive every future modal will inherit. Both fixed in one wave (`.error` → `var(--color-verdict-no)`, submit button matched to the trigger's exact styling, `max-width` dropped, `useId()`-based `aria-labelledby` wired through), plus a bundled trivial fix (misleading "polyfill must run first" comment in `vitest.setup.ts` — false under ES module import hoisting). Scoped re-review: all 3 addressed, no new breakage. Double-`onClose`-fire quirk in `Modal` (inherited from the plan's own reference code, harmless while every consumer uses idempotent `setOpen(false)`) reconfirmed inert at the cross-task level and left as a documented deferred minor rather than fixed.

**Recommendation repeated, not yet acted on:** the final review flagged this as the second instance of Story 3's "orphaned shared file" pattern — no story in the 12-story Epic 8 breakdown owns files shared *across* routes (`(dashboard)/layout.tsx` in Story 3, `TransactionForm.module.css` here, `/accounts/new` and `/recurring-rules/new` page modules still pending). Worth a single "shared form + chrome reskin" story to close all of these before Story 5. Also noted: `AddTransactionModal` is now trivially reusable and Story 5 (`/transactions` page) should consider surfacing it there too, since adding a transaction is currently only reachable from Overview.

45/45 tests pass (`web`'s full suite, no regressions). Merged to `main` locally (`7a2bedc`, fast-forward from `c6e6317`), worktree and branch cleaned up cleanly.

**Next:** mark Story 4 Done in Jira. Decide Story 5 scope (`/transactions` page — filters, density modes) or address the repeated orphaned-shared-file recommendation first.

**Story 5 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-11-transactions-page.md`, 3 tasks. Task 1: pure `web/lib/transaction-filters.ts` (`transactionLabel`, `extractCategoryLabels`, `filterTransactions`, `sortTransactionsDesc`, `groupTransactionsByDay`), unit-tested directly. Task 2: `web/components/TransactionsExplorer.tsx` — client component owning search/category/density state, consuming Task 1's pure functions. Task 3: rewired `web/app/(dashboard)/transactions/page.tsx` to fetch both `getTransactions()` and `getMyAccounts()`, build an `accountId → name` map, and render the reskinned header + `TransactionsExplorer` + the existing `AddTransactionModal`.

**Data-model decision, forced before writing the plan:** the design handoff's Transactions screen shows a separate description/merchant field and per-category chip colors; this codebase's `Transaction` type has neither (`category` is a freeform optional string, confirmed against `finance-api`'s Zod DTO — not an enum, so category chips had to be derived dynamically from the actual data, not hardcoded from the mockup's example categories). Every place the design shows a description, the implementation uses `category ?? type` instead — the same fallback already established on Overview. Documented as a deliberate, forced simplification rather than silently inventing a field.

**Worktree tooling note:** the harness's native `EnterWorktree` tool defaults to branching from `origin/<default-branch>` (`worktree.baseRef: "fresh"`), which would have forked 25 commits behind local `main` (this repo has never been pushed past a much older point) — used the manual `git worktree add` fallback instead, branching off local `main` HEAD. `.worktrees/` added to `.gitignore` (committed separately, `dfcf9ff`) since it didn't exist yet as a convention in this repo.

**Per-task reviews:** Task 1 (pure logic) and Task 2 (component) both approved clean on first pass — Task 2 had one Minor (a CSS class name typo, `dayGroup` vs `.dayGroups`, inherited verbatim from the plan's own reference code, zero visual effect) deferred to the final review rather than fixed immediately. Task 3's implementer caught a real bug in the plan's own test fixture — two fixture transactions sharing `account_id: 1` made `getByText("Revolut")` multi-match across two table rows — fixed test-only (`getAllByText(...).length > 0`), no production code touched, same "plan's test code has a bug, not the implementer" pattern seen in earlier stories.

**Final whole-branch review (most capable model) caught 2 Important cross-task issues neither task's own reviewer could see:** (1) the empty state only checked "no filtered results," so a brand-new user with zero transactions saw "No transactions match your filters." instead of "No transactions yet." — Overview's equivalent card already gets this right, this page didn't; (2) the by-day heading level jumped straight from the page's `<h1>` to `<h3>` with nothing in between, breaking the screen-reader outline (same defect class as Story 3's duplicate-`<h1>` finding). A third finding — the header text "{count} transactions · {month}" reads as if the count were scoped to that month when it's actually lifetime — was flagged as plan-mandated copy, escalated rather than silently changed; **user chose "All transactions · {month}"** over month-scoping the count or leaving it as-is. All three fixed in one wave; the fix implementer also cleanly resolved the deferred `dayGroup`/`.dayGroups` Minor as a free-win while in the same file, choosing a third option (an empty `.dayGroup {}` rule) over both literal fixes suggested in the findings doc, after correctly reasoning that either literal rename would have changed rendered spacing.

**6 Minor findings deferred to the ledger (now discarded with the workspace, recorded here since the ledger itself doesn't survive):** the search placeholder ("Search category or type") doesn't actually match on `type`, only on the derived label; a `transfer`-type transaction renders in the same lime "income" accent color as real income (pre-existing behavior carried over from the old table, brief §11 rule 1 territory, made more visually prominent by the reskin — not a regression this story introduced); the header's `Intl.DateTimeFormat("en-US")` is the only non-`ro-RO` locale call in the codebase (`lib/money.ts` uses `ro-RO` throughout); the category-chip row and density-toggle row are visually identical pill styles distinguished only by `aria-label` (fine for assistive tech, ambiguous for sighted users); filter state (search/category) silently persists across an add-transaction modal round-trip, so a newly added transaction can appear to not show up if a chip filter is still active; and a few test-coverage gaps (compact density has no dedicated test, only by-day and comfortable are exercised; the "—" unknown-account fallback isn't tested; by-day mode's row *contents* aren't asserted, only its headings).

Verified clean end-to-end: full suite 65/65 (`web`), `npm run build`, `tsc --noEmit`, and `eslint` all clean on the merged branch. Confirmed byte-identical hero-header duplication against Overview's `page.module.css` (the plan's deliberate choice not to extract a shared module didn't drift). This is the first Epic 8 reskin story that didn't inherit a hardcoded light-theme contrast leftover — the old table's `#1a7f37`/`#b3261e` colors were deleted outright rather than carried forward, unlike every prior reskin story.

Merged to `main` locally (`2ef6e06`, fast-forward from `dfcf9ff`), worktree and branch cleaned up.

**Next:** mark Story 5 Done in Jira. Decide Story 6 scope (`/accounts` page — summary + account cards) or address the still-open orphaned-shared-file recommendation (now three stories deep: Story 3's `(dashboard)/layout.tsx`, Story 4's `TransactionForm.module.css`, and arguably this story's duplicated hero-header CSS block, deliberately not extracted per the spec).

**Story 6 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-11-accounts-page.md`, 3 tasks. Task 1: `web/lib/account-metrics.ts` (`computePercentOfTotal`, `formatPercent`) — pure helpers enforcing brief §11 rule 6 (zero-denominator → explicit "unavailable", never a fabricated value). Task 2: `/accounts` page reskin — a total-balance summary card reusing the existing `KpiCard` component, plus a per-account card grid (name, balance, reference date, % of total). Task 3: folded in a related deferred item from Story 2's ledger rather than opening a new story for it — moved `web/app/accounts/new` into the `(dashboard)` route group (it was rendering with no sidebar/header, a gap flagged and explicitly deferred to "Story 6's account-creation work" back in Sprint 8's Story 2 log) and fixed `Sidebar.tsx`'s active-route match, which was exact-equality-only, to recognize nested routes like `/accounts/new` (`web/app/recurring-rules/new` has the identical gap but stays out of scope — no story owns it yet).

**Final whole-branch review (most capable model) returned 3 Important findings, all in the "plan explicitly froze a file whose surrounding context this story changed" category — decisions, not defects — so the controller ruled on all three directly per `subagent-driven-development`'s "rulings, not stalls" principle rather than stopping to ask:** (1) the moved `/accounts/new` form kept its old narrow-centered, UA-default-button CSS, reading as a different app from the reskinned `/accounts` grid it's reached from — reskinned to match (dropped the fixed-width centered layout, styled the submit button as the same lime pill `TransactionForm.module.css` already uses). (2) account creation redirected to `/` (Overview) on success, and the "back" link also pointed at `/`, even though `/accounts/new`'s only entry point in the codebase is now the `/accounts` page itself — both changed to `/accounts`. (3) `computePercentOfTotal` only guarded `totalBani === 0`; a *negative* total inverted the percentage into a misleading value (e.g. `"150.0% of total"`) — same fabricated-feeling-indicator risk rule 6 exists to prevent — guard widened to `totalBani <= 0`, with a new test covering it. One fix wave, one scoped re-review, all three verified ADDRESSED with no new breakage.

7 Minor findings parked, not fixed (per the skill, minors never enter the fix loop): the lime pill button styling is now duplicated a third time (`AddTransactionModal`, `TransactionForm`, and this page's `.addButton`) — a real candidate for a shared `.pill` global utility; the zero-accounts empty state pairs oddly with a `"RON 0,00"` KPI card; `"% of total: unavailable"` reads awkwardly as standalone copy; the total-balance KPI always renders in the "good" lime accent even if negative (same deferred item Story 3 logged for Monthly surplus); `isActiveRoute` has no pathname null-guard; one Sidebar test gap; `reference_date` renders as a raw ISO string (consistent with `/transactions`, not a new inconsistency).

**Recommendation repeated, now a fourth time:** the final review flagged this as the same "orphaned shared file" pattern noted after Stories 3, 4, and 5 — `/accounts/new`'s reskin only happened because this story's move forced the issue, and `web/app/recurring-rules/new` is now the last page both outside the dashboard shell and unreskinned, with no story owning it. Worth a small dedicated story before Story 7.

Full test suite hit real flakiness during this session (5-6 tests failing with `Error: Test timed out in 5000ms` — pure timeouts, not assertion failures, different tests each run) under load from multiple concurrent background subagents; a re-run with `--testTimeout=20000` immediately after confirmed 76/76 passing with zero code changes, and 76/76 passed again cleanly on merged `main`. Not a regression — worth noting if `npm test` intermittently times out again outside this kind of heavy-concurrency session; the suite's default 5s-per-test timeout combined with jsdom's ~21s-per-worker cold-start (17 test files, `isolate: true`) is a plausible standing flakiness risk even in normal sessions, not something this story introduced or fixed.

Merged to `main` locally (`0db458b`, fast-forward from `b169ba7`), worktree and branch cleaned up.

**Next:** mark Story 6 Done in Jira (no Jira MCP tool is available in this environment — this needs to be done by hand, or by pointing Claude's browser tool at an already-open Jira tab). Decide Story 7 scope (`recurring-rules/new` route-group fix + shared form/chrome reskin — the fourth-time recommendation above — or `/login`/`/signup` reskin, the next story in the original epic breakdown).

**Story 7 done, executed via `subagent-driven-development` in an isolated worktree — smallest story so far (1 task).** Plan: `docs/superpowers/plans/2026-09-11-login-signup-reskin.md`. Reskinned `/login` and `/signup` to the dark/lime token set — centered `.card` auth card, the lime pill submit button, `var(--color-verdict-no)` replacing a hardcoded `#b3261e` error color, minimal field-input styling matching the rest of the app. Explicitly no logic changes: `signIn`/`AuthError` handling on `/login` and `signupAction`'s `fetch`/error-handling on `/signup` are byte-identical to before — task reviewer and final reviewer each independently confirmed this via a manual whitespace-stripped diff, not just trusting the implementer's report. Final review: 0 Critical/Important findings, "Ready to merge: Yes" — cleanest story yet.

4 Minor findings parked, not fixed: form labels here use `--color-text-secondary` where `TransactionForm`/`accounts/new` leave labels unstyled (inherit `--color-text-primary`) — a genuine but debatable cross-page drift, plan-mandated; hardcoded px type sizes (same standing gap since Story 1's promised type scale never shipped); `/login`/`/signup` still have zero render-level test coverage (pre-existing, not required by the spec's Testing section); the `docs/superpowers/plans/*.md` files remain untracked on `main` across every Epic 8 story so far (a documentation-hygiene gap, not this story's to fix). Also surfaced, correctly left untouched per "no logic changes": `/login` always shows the fixed "Invalid email or password." message even for a Zod validation error (e.g. a too-short password), unlike `/signup` which surfaces the real message — worth a separate ticket.

**Recommendation repeated a fifth time:** the lime pill button style is now duplicated in 5 places (`TransactionForm`, `AddTransactionModal`, `accounts/page.module.css`'s `.addButton`, `accounts/new`, and now both auth pages) — a `.pill` global utility next to the existing `.card`/`.tabularNums` would collapse all of them. `web/app/recurring-rules/new` remains the only page still both outside the dashboard shell and unreskinned, with no story owning it.

**Process note:** the merge for this story was approved without a fresh local test run — system load spiked to 240-537 (confirmed via `ps -Ao pid,pcpu,comm -r` as Chrome/VS Code/WindowServer, not leftover subagent processes) partway through the session, and 4 separate attempts to run the full Vitest suite locally all failed (timeouts, worker spawn errors, one run producing 0 completed tests). Proceeded on 3 independent clean 76/76 runs already on record for the exact merged commit (`325f120`): the task implementer, the task reviewer, and the final whole-branch reviewer each ran `build`/`tsc`/`eslint`/`vitest` clean minutes earlier, before the load spike — user explicitly chose this over waiting. Worth a real local re-run once the machine is quiet again, just to close the loop, though nothing about this points at the code itself.

Merged to `main` locally (`325f120`, fast-forward from `4f3c757`), worktree and branch cleaned up.

**Next:** mark Story 7 Done in Jira (same manual-step note as Story 6). Re-run `npm test` locally once system load is normal, just to confirm cleanly on `main` (expected: still 76/76, no code reason to expect otherwise). Decide Story 8 scope — candidates: the now-5x-repeated shared-chrome cleanup (`.pill` utility + `recurring-rules/new` route-group fix), or continuing the original epic breakdown (Story 8: Analytics Service daily-balance-series extension, the first of the Forecast-track stories).

Re-ran `npm test` locally with the machine quiet: 76/76 clean, closing Story 7's open loop — confirms the load-spike flakiness noted there really was environmental, not a code issue. **User chose Story 8 = Analytics daily-balance series** (continuing the original epic breakdown) over the shared-chrome cleanup.

**Story 8 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-11-analytics-daily-balance-series.md`, 2 tasks. Task 1: `analytics-service/forecast.py`'s `ForecastResponse` gains `daily_balances: list[DailyBalance]` (`DailyBalance = {date, balance: Decimal}`), one entry per day in `[calculation_date, window_end_date]` inclusive (31 entries for the 30-day window). `compute_forecast` refactored from "accumulate straight into a scalar" to a two-pass design: build `deltas_by_date` via the existing `_occurrences_in_window`/`_clamped_occurrence` helpers (unchanged, still the only occurrence-calculation implementation in Python), then walk day-by-day applying each day's delta. `forecast_balance` is now `daily_balances[-1].balance` — derived, not computed twice, so the two values can't drift apart. The exclusive-window-end semantic falls out of the existing helper for free: `_occurrences_in_window` never returns `window_end` itself, so the final day's delta is always zero and the last point automatically matches the old exclusive-window sum, with no special-case code needed. Task 2: `web/lib/analytics.ts`'s `Forecast` type gains `dailyBalances: {date: string; balance: string}[]` — a pure type mirror, no consumer code changes yet (Story 9's `ForecastChart` is the first reader).

Both tasks approved clean at task review. **Final whole-branch review found 2 Important issues neither task's own reviewer could see:** (1) the same-day rule-accumulation branch (`deltas_by_date[occurrence] = existing + signed_amount`, needed when two rules land on one date, e.g. rent + salary both on the 15th) had zero test coverage — every existing test used at most one rule. The reviewer proved this by mutation testing: swapping accumulation for a plain overwrite still passed all 15 tests, meaning a bug that silently drops one of two same-day rules was invisible to the suite — a real money-losing failure mode with no guard. (2) The `assumptions` list (rendered verbatim to the user on the dashboard) never explained that the daily series' last point can never carry an occurrence, since the interval is exclusive exactly at `window_end_date` — brief §11 rule 7 territory once a chart starts plotting the series. Both ruled on directly per `subagent-driven-development`'s "rulings, not stalls" (neither is a stop condition — not destructive/security/shared-side-effect/unresolvable): fixed in one wave (added the same-day accumulation test, appended one line to `assumptions`), one scoped re-review confirmed both addressed with no new breakage. 16/16 Python tests pass, 76/76 web tests pass, both verified again after the final merge to `main`.

**Minor findings parked for Story 9** (which will be the first code to actually read `dailyBalances`): `web/app/(dashboard)/page.spec.tsx`'s `getForecastMock` fixture is missing the new field (untyped mock, won't fail until Story 9 reads it — add `dailyBalances: []` or type the mock against `Forecast`); no exported `DailyBalance` TS type yet (Story 9 will want to name a single point's type); the day-loop in `forecast.py` is an unbounded `while True` rather than a bounded `for` (low risk, `FORECAST_WINDOW_DAYS` is a fixed positive constant, but now a second instance of the pattern); no automated test asserts the wire shape is camelCase/string-typed (verified manually via `TestClient` during review); `docs/superpowers/plans/*.md` remains untracked on `main`, same standing gap as every prior Epic 8 story.

Merged to `main` locally (fast-forward, `2ff2860`..`668da37`), worktree and branch cleaned up (one untracked plan-doc file blocked `git worktree remove` on first attempt — confirmed it was a duplicate of the already-untracked copy on `main`, not unique work, before deleting it and retrying).

**Next:** mark Story 8 Done in Jira. Decide Story 9 scope (`ForecastChart` component — Line/Weeks/Calendar modes, the first consumer of `dailyBalances`) — remember the two parked Minors above (stale mock fixture, no exported `DailyBalance` type) when starting it.

**Story 9 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-14-forecast-chart-component.md`, 7 tasks. Recharts added as a dependency for the first time in this project, pinned exact (`3.10.1`, not a range) — same convention as Prisma's exact pin, and it pulled 39 transitive packages into `dependencies` (`@reduxjs/toolkit`, the `d3-*`/`victory-vendor` graph), flagged for Story 10 to consider `next/dynamic` on since Overview will embed Line mode in its first-load JS. jsdom implements neither `ResizeObserver` nor a non-zero `getBoundingClientRect` (confirmed by checking directly), both needed for Recharts' `ResponsiveContainer` to render anything in tests — Task 1 added self-disclosed, always-on polyfills to `vitest.setup.ts` (same guarded-but-effectively-unconditional pattern as the earlier `HTMLDialogElement` polyfill).

Two pure-logic modules (`web/lib/forecast-chart-data.ts` — series/week-bucket math; `web/lib/forecast-occurrences.ts` — a clamped monthly rule-occurrence lookup, a fourth independent client-side reimplementation of the day-of-month clamp/window convention Python's `_occurrences_in_window` established in Sprint 6, deliberately narrow and display-only) feed three mode components (`ForecastChartLine.tsx` — Recharts, dashed/solid segments structurally split for a future historical-actuals feature that's always empty today, custom HTML tick labels via `foreignObject` per the design's "not SVG text" requirement, Today/Low reference lines; `ForecastChartWeeks.tsx` — 5 hand-built bar-pair buckets; `ForecastChartCalendar.tsx` — a hand-built 16-column day-grid plus rule-day cross-reference cards), dispatched by a thin `ForecastChart.tsx`. Continued this project's "no `Date` object" discipline (now a fourth enforcement layer, joining `pg`'s DATE parsing, Prisma's serialization, and Analytics Service's Pydantic layer) — every new date computation is raw `YYYY-MM-DD` string slicing/comparison, verified clean by the final reviewer's own grep, not just asserted.

**A real plan bug surfaced and ruled on mid-loop (Task 2):** the plan's own literal reference code for `groupIntoWeeks` and the plan's own reference test were mutually contradictory (hand-verified independently by both the task reviewer and the controller) — the implementer's `points.length <= 7 ? ... : Math.ceil(...)` branch was the correct fix, not an unauthorized deviation, and is behavior-identical to the plan's literal formula for the only input real production data ever produces (31 points, both give bucketSize 7).

**Task 4 (Recharts Line mode) needed one fix round:** the implementer's first pass added an unrequested `interval={0}` to the x-axis to route around a test-environment artifact (the shared `getBoundingClientRect` polyfill made every tick label look 600px wide under Recharts' default auto-skipping), which would have forced all 31 real day-ticks to render on a ~280px-tall chart in production — an unrequested behavior change to fix a test, not the component. Reverted; the test was corrected instead to assert on the tick Recharts' own default behavior actually keeps under jsdom, leaving production behavior untouched. This is the same "fix the reference test, not production code" principle the implementer had already correctly applied to an unrelated regex-collision bug in the same task.

**Final whole-branch review (opus) found 2 Important cross-task issues no single task's reviewer could see, both ruled on and fixed in one wave:** (1) Weeks mode's in/out bars skipped day 0's delta (no prior point to diff against), but `dailyBalances[0]` already bakes in any rule occurring exactly today — so a same-day rule was invisible in Weeks mode's bars while still reflected in that same card's end balance, and Calendar mode (which correctly uses `occurrencesInWindow`) showed a rule-day card for the identical occurrence: two modes of one component disagreeing about the same data, a real instance of the standing "never render a silently wrong indicator" rule, reachable under ordinary use (any rule dated today), not an edge case. Ruled to fix now rather than defer to Story 10: `groupIntoWeeks` rewritten to compute each bucket's in/out from actual `occurrencesInWindow` results scoped to that bucket's date range, matching the spec's own wording and matching Calendar's method — eliminates the blind spot structurally instead of patching around it. (2) `ForecastChartLine`'s custom Recharts dot renderer typed its payload as `DailyBalancePoint` (`{date, bani}`) when Recharts actually passes the chart-data row shape, advertising a `bani` field that was always `undefined` at runtime in a money component 3 future stories will edit — retyped to only claim what's read. One fix wave (`a8b863f`), one scoped re-review independently hand-verified both fixes (including re-deriving the day-0 test case by hand and re-running the touched spec files), confirmed addressed with no new breakage.

**8-item deferred-minors list triaged by the final reviewer, 7 left parked as pre-existing/inert** (hardcoded px spacing in `ForecastChartWeeks.module.css` — standing project-wide gap since Story 1's promised type/spacing scale never shipped, now flagged in 7 consecutive stories; array-index React keys on fixed-length arrays; a doc-comment/dead-fallback nit in the accepted `groupIntoWeeks` branch; `ForecastChartProps` requiring `recurringRules` even for modes that don't use it, a deliberate tradeoff), **1 promoted to a Story 10 pre-flight note rather than fixed here:** no mode has a test for an empty `dailyBalances` array (each degrades differently — Line renders an empty chart silently, Weeks renders 5 "—" placeholders, Calendar renders nothing — none crash, but the contract isn't pinned down; can't happen today since the backend always emits 31 points, but Story 10 should decide and test it once a real page can pass `[]`).

**Two Story-8-parked minors carried forward, still open, explicitly re-surfaced by the final reviewer as a Story 10 landmine, not resolved by this story:** `web/app/(dashboard)/page.spec.tsx`'s `getForecastMock` fixture still doesn't include `dailyBalances` — harmless while nothing reads it, but will start failing the moment Story 10 wires `ForecastChart` into Overview; and no exported `DailyBalance`/`{date,balance}` wire type exists yet — the literal `{date: string; balance: string}[]` shape is now hand-written 9 times across the new files (grep-confirmed by the reviewer), worth collapsing into one exported type before Story 10 adds a 10th copy.

Recharts exercised through a real `npm run build` for the first time (Task 7) — clean, first proof its ESM/CJS interop and `"use client"` boundary work outside Vitest/jsdom. 113/113 web tests pass (up from 76 before this story), `tsc --noEmit`/`eslint` clean. **Process note:** the `main` checkout's own `web/node_modules` had never had `npm install` run for the new `recharts` dependency (only the worktree had it) — 2 of 23 test files failed after the fast-forward merge with `Failed to resolve import "recharts"` until `npm install` was run on `main` itself; re-ran clean (113/113) immediately after. Worth remembering as a standing step whenever a worktree-based story adds a new dependency: the merge doesn't install it into the main checkout automatically.

No page imports `ForecastChart` yet — deliberately out of scope (spec's epic breakdown puts "`/forecast` page + Overview chart embed" in Story 10). Merged to `main` locally (fast-forward, `7db79f4`..`a8b863f`), worktree and branch cleaned up (same untracked-plan-doc-blocks-`git worktree remove` pattern as Story 8, confirmed duplicate before deleting and retrying).

**Next:** mark Story 9 Done in Jira. Decide Story 10 scope (`/forecast` page + Overview chart embed, the first real consumer of `ForecastChart`) — remember the Story-10 pre-flight list above: export a `DailyBalance` type, update the stale `getForecastMock` fixture before it starts failing, decide and test each mode's empty-`dailyBalances` contract, and consider `next/dynamic` for Line mode's bundle weight.

**Story 10 scoped via brainstorming (bounded path — refinement of the already-approved epic spec, no new spec file).** Decisions: Overview's forecast card becomes the full `ForecastChart` with mode switcher (not line-only, not the old text summary); mode-switcher state is local per page (`useState`, no sharing/URL/persistence); empty `dailyBalances` (thrown fetch or successful-but-empty) collapses to one shared "unavailable" message, no per-mode degrade paths; `/forecast` gets 3 KPI cards (current balance, balance in 30 days, lowest projected + date) plus chart+switcher+assumptions footer, no recurring-rules table (deliberate deviation from the epic spec's Story 10 line, which also asked for that table — recurring rules stay Overview-only); `next/dynamic({ssr:false})` wraps `ForecastChart` on both pages to keep Recharts out of initial JS. **Real gap caught before planning:** the epic spec's own Story 10 line (mocked KPI cards) wasn't in the first draft of the brainstormed design — surfaced to the user, who chose to add them rather than defer.

**Story 10 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-14-forecast-page-overview-embed.md`, 5 tasks. Task 1: centralized `DailyBalance = {date, balance}` (hand-duplicated 9x since Story 9) into one export in `web/lib/analytics.ts`. Task 2: `ForecastUnavailable`, a shared empty-state message. Task 3: `ForecastSection` — the piece both pages actually embed: owns local mode-switcher state, lazy-loads `ForecastChart` via `next/dynamic`, renders `ForecastUnavailable` on null/empty data, renders the assumptions/calc-date/formula-version footer (brief §11 rule 7) unconditionally whenever a chart renders. Task 4: new `/forecast` page — auth-gated, 3 KPI cards, `ForecastSection` below. Task 5: Overview's old inline text-only forecast card replaced with the same `ForecastSection`; Sidebar's "Forecast" item flipped from disabled to a real link.

All 5 tasks individually reviewed clean, no fix loops needed at the task level (one legitimate no-op deviation in Task 4: an implementer dropped an unused `formatAmount` import that was a genuine bug in the plan's own reference code, verified by the task reviewer as not a missed requirement).

**Final whole-branch review (opus) caught 2 real Important cross-task issues no single task's reviewer could see, both fixed pre-merge in one wave:** (1) `/forecast`'s "Current balance" KPI used `dailyBalances[0]`'s value (which already has today's rule occurrences applied by the backend) instead of the plain current total — meaning it could silently show a different number than Overview's "Total balance" KPI for the same instant, whenever any rule lands exactly today. Fixed to use `totalBani` directly, matching what's actually sent to Analytics Service. (2) `ForecastUnavailable`'s fixed copy ("Analytics Service could not be reached") is asserted even on the empty-but-succeeded path this story itself added — a false claimed cause on real user-facing copy, worse than the generic "unavailable/explained" rule 6 asks for. Fixed to a neutral message ("Forecast unavailable right now. Try again later.").

**Controller ruled directly on 2 more items rather than looping:** a product-decision finding (Overview lost the "sold estimat" headline number entirely once its old text card was replaced by a chart-only embed — plan-sanctioned per the brainstorming decision, but the full effect probably wasn't the intent) — restored the number inside Overview's own JSX specifically (not inside the shared `ForecastSection`, since `/forecast` already has an equivalent KPI card and would've shown it twice). And a spec-assigned test-coverage gap (the epic spec explicitly assigns `/forecast` route-protection test coverage to "stories 10/12" — added the two missing `isPathAuthorized` cases, no behavior gap existed since the guard is deny-by-default).

One fix wave (`b68b8e7`), one scoped re-review: all 4 findings verified ADDRESSED, no new breakage. 9 Minor findings parked, none load-bearing (mode-switcher pill styling not matching the codebase's established toggle-pill pattern from `TransactionsExplorer`; a test asymmetry where the empty-`dailyBalances` case checks less than the null case; KPI tests asserting labels but not values; a couple of dead fallback branches under an already-narrower guard; a test-timing comment that plausibly misattributes its root cause to `next/dynamic` when a raw `.click()` bypassing RTL's `act()` wrapper is the more likely explanation, inherited verbatim from the plan's own reference test code; missing `aria-live` on the empty-state/skeleton components, confirmed inert since neither ever swaps in client-side after initial render; a missing heading on `/forecast`'s chart section; `DailyBalance` living in a `server-only` module reached by 6 client files via type-only imports, which works today but would need to move if a runtime value is ever added alongside it; Overview's chart isn't literally "smaller" than `/forecast`'s per one spec line, already superseded by this story's own brainstormed decision to give Overview the full switcher).

127/127 tests pass on merged `main` (up from 76 before Story 9, 121 after Task 4, 127 after the fix wave — 14 new tests this story). Merged to `main` locally (fast-forward, `24c594f..b68b8e7`), worktree and branch cleaned up (same untracked-plan-doc-blocks-`git worktree remove` pattern as Stories 8-9, confirmed duplicate before force-removing).

**Recharts is now live on two routes** (`/forecast` and Overview) for the first time, both behind `next/dynamic` — worth watching real bundle-size numbers in a future `npm run build` output if this becomes a concern, though nothing in this story's review flagged it as one yet.

**Next:** mark Story 10 Done in Jira. Decide Story 11 scope (Simulator, roadmap stage 6, the next unbuilt piece of Epic 8's 12-story breakdown) — remember `ForecastUnavailable`'s copy is generic today specifically because there's only one call site; the epic spec's own plan already anticipates Simulator becoming a second `getForecast` caller with its own reason to explain, so revisit whether the message needs a `reason`/`detail` prop once that lands.

**Story 11 scoped via brainstorming (bounded path — refinement of the already-approved epic spec, no new spec file).** User chose to bundle the epic spec's items 11 (calc engine) and 12 (page + Overview embed) into one story rather than split them — same merge pattern as Sprint 8 itself. One real design fork surfaced and put to the user before planning: the epic spec's "4th independent reimplementation" language for the simulator's occurrence math predates Story 9, which already built a TS occurrence engine (`forecast-occurrences.ts`) for the chart — reusing it would make it the *same* implementation, not independent. **User chose to write a second, separate occurrence engine** (a real cross-check between simulator and chart) over reusing the existing one.

**Story 11 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-14-simulator.md`, 4 tasks. Task 1: `web/lib/simulator.ts` — pure calc engine, independently reimplements the monthly-rule occurrence/clamp math (own function names, not imported from `forecast-occurrences.ts`), builds base/after-purchase daily series, derives a Yes/Tight/No verdict from a single named threshold. Task 2: `SimulatorPanel` — full `/simulator` UI (amount input+slider, 4 preset chips, freetext note, verdict banner, 3-way comparison view side-by-side/deltas/overlaid — overlaid reuses `ForecastChart`'s existing `afterSeries` prop unchanged, a real payoff from Story 9 having built that prop speculatively). Task 3: `/simulator` page (auth-gated, inside the `(dashboard)` route group from the start — avoiding the repeated `/accounts/new`-style mistake) + Sidebar's Simulator link enabled + route-protection coverage. Task 4: `SimulatorCard`, a condensed Overview embed reusing the same engine directly (not `SimulatorPanel`), replacing Story 3's placeholder.

**Task 1's implementer deviated from the brief on both points the review was told to specifically check:** wrote the occurrence window as exclusive-start instead of the mandated inclusive-start `[start, end)` convention (to satisfy its own mislabeled test, rather than fixing the test — a same-day recurring rule would have been silently dropped from the simulation), and used float multiplication for the verdict threshold (`currentBalanceBani * TIGHT_THRESHOLD_RATIO`) — copied verbatim from the plan's own reference code, a real self-inconsistency in the plan text against the project's standing "no floats for money" rule. Both ruled on and fixed in one round: inclusive-start restored, threshold rewritten as an exact integer numerator/denominator cross-multiplication. **The identical float-money defect recurred in Task 2** (`SimulatorPanel`'s amount-input parser, same plan-text bug) — fixed there via `toBani`, and the fix was carried forward preemptively into Task 4's dispatch so `SimulatorCard` never got the chance to repeat it a third time.

**Final whole-branch review (opus) caught 3 Important cross-task findings, all fixed in one wave:** (1) both new amount inputs were `type="number"` with a derived `value={amountBani/100}` — typing a decimal point produced an in-progress string that browsers sanitize to `""`, which React then wrote back as `0`, making it structurally impossible to type any decimal amount (verified against jsdom's value-sanitization and React 19's controlled-input restore code, not inferred). Fixed by switching both components to raw string state, matching the pattern `TransactionForm.tsx` already established. (2) `simulator.ts`'s default `calculationDate` fell through to a client-side `new Date()`, executing during SSR and again on hydration in two ordinary (non-`ssr:false`) client components — an untested code path and a latent hydration-mismatch/inconsistent-window risk. Fixed by exporting `todayDateString` and having both Server Component pages compute and pass it explicitly. (3) `/simulator` skipped from `<h1>` straight to `<h3>` (the stat-card titles) — same defect class Story 5's final review already caught once. Fixed to `<h2>`.

One fix wave, one scoped re-review: all 3 ADDRESSED, no new breakage — the reviewer independently re-verified the preset chips' `aria-pressed` comparison and the slider/text-field two-way sync still held after the string-state refactor. A cross-check the story's own design justified is now actually possible but not yet enforced: `simulator.ts`'s `baseSeries[30]` and the Analytics Service's `forecastBalance` should be numerically identical for the same inputs, but nothing currently asserts it — flagged for a future story, not fixed here. Minor findings parked: `VERDICT_LABEL` copy duplicated with slightly different wording between the two components; the verdict can blame a purchase for a shortfall a pre-existing expense rule already caused (spec-acknowledged as provisional, pending Settings' essential-spend field); the overlaid chart's two lime lines have no legend distinguishing base from after; a few dead unreachable fallbacks and untested branches (negative `?amount=`, "tight" verdict label, deltas-view cell values); hardcoded px spacing (9th consecutive story flagging this).

**One more test-only fix made directly during branch-finish verification** (not part of the SDD loop, which had already closed): the "switches to the overlaid view and renders the chart" test failed reproducibly (3/3 full-suite runs, always the same test, passed cleanly in isolation) — RTL's `waitFor` default 1000ms timeout racing the `next/dynamic` lazy import under 29 concurrent jsdom environments. Different from this project's earlier documented flakiness pattern (Story 6: different tests failing each run, pure environmental noise) — this was deterministic, so fixed properly with an explicit longer `waitFor` timeout rather than shrugged off.

150/150 tests pass on merged `main` (up from 127 before this story). Merged to `main` locally (fast-forward, `b68b8e7..b279379`), worktree and branch cleaned up (same stale-registration-after-directory-already-gone pattern as Story 3 — `git worktree remove --force` + prune, nothing lost since the directory no longer existed on disk).

**Roadmap stage 6 (Simulator) is now built.** Epic 8 (dark/lime reskin + Forecast + Simulator) is 11 of 12 stories done — only the epic spec's own Story 12 line (which this story's bundling decision folded in) remains unaccounted for; nothing left unbuilt from the original 12-story breakdown.

**Next:** mark Story 11 Done in Jira. Epic 8 is functionally complete (Settings + CSV import, sub-project 5, remains explicitly out of scope per the original spec) — decide whether to start sub-project 5 next, revisit the `.pill`/shared-chrome cleanup recommendation (now repeated across many stories), or close Epic 8 and move to roadmap stage 7 planning.

**Epic 8 closed via user decision: sub-project 5 (Settings + CSV import) started next, as Epic 11**, roadmap stage 7 — no explicit Jira epic-close/stage-7-planning administrative pass done separately, user chose to go straight to scoping. Spec brainstormed and written: `docs/superpowers/specs/2026-09-15-settings-csv-import-design.md`. Key decisions: kept as one epic with sequential stories (same merge pattern as Sprint 8/Epic 8), not split into two epics. Profile card scoped down to email-readonly + RON note (no `displayName` — YAGNI, nothing else would consume it). Forecast assumptions (`essentialSpend`/`payday`/`horizonDays`) all made functionally real, not stored-only placeholders — `payday` anchors the forecast/simulator window to `[today, next payday)` when set (payday-priority-over-horizon precedence, chosen over a `min()` of both to avoid doubling window-computation logic at every call site), revising the CLAUDE.md-documented fixed `[azi, azi+30)` "sold estimat" window (see updated definition above). Data card (Export CSV, Delete account with password re-confirmation) both made real, not placeholders. CSV import targets Revolut's real personal-account statement format (researched via web search — `Type,Product,Started Date,Completed Date,Description,Amount,Fee,Currency,State,Balance`; only `State=COMPLETED` rows import; `Amount` sign maps directly to income/expense, Revolut `TRANSFER` rows are NOT modeled as Saldovio's own `transfer` type since the destination account isn't Saldovio-tracked), with dedupe via a hash of file-verbatim fields (never a fabricated bank ID, per brief §11 rule 3). Analytics Service simplified: `ForecastRequest.window_end_date` replaces the internal `FORECAST_WINDOW_DAYS` constant, making `web` the single owner of window-boundary logic. 5-story epic breakdown: S1 (schema+Settings API+window logic), S2 (Settings UI), S3 (Simulator wiring), S4 (CSV import backend), S5 (CSV import UI+export+delete).

**Story 1 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-15-epic11-story1-settings-schema-window.md`, 4 tasks. Task 1: `User` gains `essentialSpend`/`payday`/`horizonDays` (Prisma migration), guarded `GET`/`PATCH /users/me/settings` mirroring the established DTO-in/snake_case-response convention. Task 2: `analytics-service/forecast.py`'s `ForecastRequest` gains a required `window_end_date` (replacing `FORECAST_WINDOW_DAYS=30`), with a `model_validator` rejecting `window_end_date < calculation_date`. Task 3: `web/lib/forecast-window.ts`'s `computeWindowEnd(today, payday, horizonDays)` — a 5th independent date-string-math module (no `Date` object), matching this project's standing "reverify date/money logic at every layer" pattern already established by `simulator.ts`/`forecast-occurrences.ts`. Task 4: wired `web/lib/settings.ts` (new) + updated `getForecast`'s signature into both Overview and `/forecast`.

All 4 tasks individually reviewed clean (Task 1 had 1 Important finding, parked as plan-mandated — `getSettings`/`updateSettings` 500s on a since-deleted user id, same already-acknowledged "no global exception filter in finance-api" gap from Sprint 7 S1).

**Final whole-branch review (opus) caught 4 real Important cross-task issues no single task's reviewer could see, all fixed in one wave:** (1) removing `FORECAST_WINDOW_DAYS` removed the only structural bound on window length at all 3 layers — `horizonDays` had no upper bound, meaning a large value would spin an unbounded loop in `web/lib/forecast-window.ts` and OOM-risk the shared, unauthenticated Analytics Service process; not reachable today (no UI yet), but Story 2 ships the form that makes it reachable. Fixed at 3 layers: Zod `.max(365)`, an Analytics Service validator ceiling (366 days), and a new Postgres `CHECK` constraint (`users_payday_check`/`users_horizon_days_check`) — matching the exact hand-appended-SQL precedent Sprint 7 already established for `recurring_rules.day_of_month` (Prisma's schema language still can't express `CHECK`). (2) Both forecast pages still said "30 days" in prominent copy (`<h2>30-day forecast</h2>`, a KPI labeled "Balance in 30 days") — now capable of being false once `payday` is set, producing a card whose own caption shows a different date than its label claims. Fixed by dropping the day-count claim (headings/labels now generic; the real date was already correct in captions/assumptions and is unchanged). (3) `getMySettings()` sat outside the forecast's fault-isolation `try/catch` (in the bare `Promise.all` with accounts/recurring-rules/transactions) — combined with Task 1's parked 500-on-deleted-user finding, a settings outage would have blanked the whole dashboard instead of degrading to "Forecast unavailable" like every other forecast-path failure. Fixed by moving the fetch inside the existing `try`. (4) Nothing tested the actual `settings.payday`/`settings.horizon_days` → `computeWindowEnd` → `getForecast` wiring — every piece was unit-tested in isolation only, so a transposed-argument bug would have compiled and passed every existing test. Added one wiring assertion per page, computed against the real `computeWindowEnd` function rather than a hand-typed literal.

One fix wave (`1dde6c2`), one scoped re-review: all 4 findings independently verified ADDRESSED (re-reviewer spot-checked the new DB `CHECK` constraint live via `psql`, not just trusting the fix report), no new breakage. 6 Minor findings parked (2 clock reads per forecast — `todayDateString()` sampled twice, ~1/86400 midnight-rollover risk, deferred to Story 3; the FK `NO ACTION`→`RESTRICT` semantics change Task 1 already flagged, reviewer's explicit "acceptable, merge as-is"; the migration-checksum repair, same "nothing further needed" verdict; `essential_spend` fetched but unread until Story 3; no automated 401 test on the 2 new routes, a standing project convention not a regression; `forecast-window.ts` re-implementing date helpers already in `simulator.ts`, deliberate per the project's standing pattern).

**Real sequencing hazard flagged by the final reviewer, not yet acted on:** `web/lib/simulator.ts` still hardcodes `SIMULATOR_WINDOW_DAYS=30` (Story 3's job to fix) — so the moment Story 2 ships the Settings form, a user who sets `payday` will see Overview's forecast card and simulator card disagree about the window, the same defect class Epic 8 Story 9's review already caught once (Weeks-vs-Calendar disagreement). Worth deciding before Story 2 starts: ship Stories 2+3 together, or have Story 2's form carry an explicit caveat that the simulator still uses a fixed 30 days.

235/235 tests pass on merged `main` (160 web + 56 finance-api + 19 analytics-service). Merged to `main` locally (fast-forward, `e6c0b95..1dde6c2`) — hit the same "worktree's generated Prisma client doesn't propagate to the main checkout" issue Story 9 hit for a new npm dependency, this time for the changed `schema.prisma`: `finance-api`'s tests failed on `main` post-merge (`Unknown field essentialSpend`) until `npx prisma generate` was re-run there. Re-ran clean immediately after (56/56). Worktree and branch cleaned up.

**Next:** mark Story 1 Done in Jira. A real logged-in browser pass on `/` and `/forecast` (byte-identical for an untouched user, then setting `payday` and confirming the shorter window renders end to end) is still recommended — unverified by any means so far, since Task 4 couldn't run a full local stack and substituted a curl-based check of the finance-api↔analytics-service contract. Decide Story 2 scope (`/settings` UI — Profile + Forecast assumptions cards, Sidebar link) — remember the Story 3 sequencing hazard above before shipping Story 2's form alone.

**Story 2+3 bundled via brainstorming (bounded path, no new spec file) — user chose to ship them together specifically to close the sequencing hazard flagged at the end of Story 1** rather than shipping Story 2's form alone with `web/lib/simulator.ts` still hardcoding a fixed 30-day window.

**Story 2+3 done, executed via `subagent-driven-development` in an isolated worktree.** Plan: `docs/superpowers/plans/2026-09-15-epic11-story2-settings-ui-simulator-window.md`, 3 tasks. Task 1: `web/lib/simulator.ts`'s `simulatePurchase` gains an optional `windowEndDate` param (falls back to the old fixed-30-day computation when omitted — byte-identical default path). Task 2: `/settings` page (Profile card: email + RON note; Forecast assumptions form: essentialSpend/payday/horizonDays), `updateSettingsAction` mirroring the established `createAccountAction`/`createRecurringRuleAction` redirect shape, `web/lib/schemas/settings.ts` (blank-string-to-`null` preprocessing for the two optional fields, `horizonDays` capped at the same 365 ceiling as the 3 backend layers), Sidebar's Settings link enabled. Task 3: threaded `computeWindowEnd(today, settings.payday, settings.horizon_days)` into both the Overview and `/simulator` pages' simulator components (same pattern Story 1 already used for the forecast pages), and renamed the now-inaccurate "Balance in 30 days" labels (`SimulatorCard`, 2 spots in `SimulatorPanel`, the `/simulator` hero subtitle) to generic copy.

**Task 2 review found one real gap:** no `page.spec.tsx` existed for `/settings`, unlike every sibling `(dashboard)`-group route this epic has added — a plan omission (the brief never asked for one), not an implementer defect. One fix round: added 5 tests (login-redirect, email display, form prefill, saved/error banners), verified ADDRESSED.

**Final whole-branch review (opus) caught a real Critical bug this story's own change exposes, plus a real plan-vs-spec scope gap — both fixed in one wave:** (1) **Critical** — `web/lib/simulator.ts`'s `monthlyRuleOccurrences` had a hardcoded `monthsChecked < 3` loop bound, a leftover invariant from when the simulator window was fixed at ~30 days (justified by its own comment: "a simulator window is at most ~31 days ... 3 is never load-bearing"). This story is exactly what makes that false — `horizonDays` up to 365 now reaches the simulator's `windowEndDate` — so a user setting a multi-month horizon would get monthly recurring rules silently truncated after 3 occurrences, producing a wrong verdict/balance and disagreeing with Analytics Service's own unbounded occurrence loop for the same window. Fixed by growing the bound to 400 (a safety net against a malformed/never-satisfied window, not a real limit — the actual exit condition was already the `candidate >= windowEnd` break) and adding a >3-month test. (2) The identical stale cap existed in `web/lib/forecast-occurrences.ts` (introduced in Story 1, not this story, but this story is what makes it reachable through the UI) — fixed the same way, so the Forecast chart's Weeks/Calendar modes no longer silently disagree with its own Line mode at a long horizon.

**Real plan-vs-spec gap, confirmed against `docs/superpowers/specs/2026-09-15-settings-csv-import-design.md`:** the approved spec explicitly assigns this story (its own "Story 3") an `essentialSpend`-based Simulator verdict threshold ("replaces the provisional 10%-of-balance rule when set, falls back to the existing 10% rule when unset") — the plan this session wrote silently narrowed Story 3 to only the window-boundary half, and `essential_spend` was being fetched on 3 pages and read by nothing. **Ruling: implemented it in the final-review fix wave rather than deferring** — the spec is the binding authority over the plan, and shipping a labelled Settings field that visibly does nothing contradicts this project's own repeated "no inert/fabricated indicator" pattern. `simulatePurchase` gained an `essentialSpendBani?: number | null` param (non-null → direct bani comparison against it; null/omitted → the exact old 10% cross-multiplication, unchanged), wired through `SimulatorCard`/`SimulatorPanel` and both pages exactly like `windowEndDate` was.

One fix wave, one scoped re-review (opus, arithmetic hand-verified both branches): all 4 findings ADDRESSED, no new breakage. 194/194 tests pass (up from 182 pre-fix-wave). 3 Minors parked: `essentialSpend = 0` is a valid stored value and silently disables the "tight" verdict entirely (an edge case, not a data-correctness bug); the new essentialSpend-based verdict isn't explained on screen yet (brief §11 rule 7 territory — real gap, deferred, not a fabricated-value violation since it's additive UX polish); one wiring test asserts `windowEndDate` but not the parallel `essentialSpendBani` on the same code path. Also carried forward from earlier task reviews: dead `aria-disabled` Sidebar branch (no `NavItem` is disabled anymore — the repo's one remaining lint warning), `/settings` missing the shared `.hero` block every sibling page has, the fetch-settings-then-compute-window idiom now hand-copied in 3 pages (a `getWindowEndDate()` helper candidate), settings fetch serialized outside `Promise.all` on 2 pages, a rejected settings save discarding the user's typed values (matches existing convention, not a regression), `?saved=1` persisting across a plain refresh, `essentialSpend` accepting more than 2 decimals client-side (inert until now, worth a `.multipleOf(0.01)` given it's load-bearing as of this story).

**Repo-state note:** `origin/main` on GitHub is 76 commits behind local `main` — the last real PR merge was #18 (Sprint 7 S3, route protection); every story since (all of Epic 8 and Epic 11) has been merged to `main` locally only, never pushed. Flagged to the user before this merge rather than assumed away; user chose local-only merge again, consistent with actual practice these last ~15 stories despite the older "PRs via `gh` CLI" standing preference. Merged to `main` locally (fast-forward, `1dde6c2..13424a2`), worktree and branch cleaned up.

**Roadmap stage 7 (Epic 11) status:** S1-S3 of the 5-story breakdown done (schema+window, Settings UI, Simulator wiring). S4 (CSV import backend) and S5 (CSV import UI + export + delete) remain.

**Next:** mark Story 2+3 Done in Jira (as S2 and S3, or as a combined entry — user's call). Decide whether to fix the 3 parked Minors before S4 (the essentialSpend-verdict-not-explained-on-screen one is the most user-visible) or move straight to S4 scoping (CSV import backend, Revolut format).

**3 parked Minors from S2+3 addressed, TDD throughout (RED confirmed before every GREEN).** (1) `essentialSpend = 0` — checked, not a bug: a 0 floor makes the "tight" zone empty by construction (any non-negative outcome is "yes", same as any other floor value below the achieved minimum), so no change made. (2) The real fix: `simulatePurchase` (`web/lib/simulator.ts`) now returns a `thresholdBasis: "essential-spend" | "balance-percent"` field alongside `verdict` — pure, tested in `simulator.spec.ts`. Both `SimulatorPanel` (full `/simulator` page) and `SimulatorCard` (Overview embed) render a one-line explanation under the verdict banner ("Based on your essential spending floor of X RON." vs. "Based on the default 10% of your balance..." / "Based on essential spend (Settings)." vs. "Based on 10% of balance." for the condensed card) — closes the brief §11 rule 7 gap (verdict basis wasn't shown). `SimulatorCard.spec.tsx` created (didn't exist before). (3) Widened 3 existing wiring tests (`(dashboard)/page.spec.tsx` x2, `(dashboard)/simulator/page.spec.tsx` x1) that asserted `windowEndDate` alone on `simulatePurchaseSpy` calls to also assert `essentialSpendBani` on the same call.

Also regenerated `.next/types` (`next build`) after an unrelated stale-cache `tsc` error (`Cannot find module '../../app/page.js'`) surfaced while verifying — not caused by this change, `app/page.tsx` was already gone (moved to `(dashboard)/page.tsx` in Sprint 8 Story 2); worth remembering that deleting `.next` requires a `next build`/`next dev` pass before `tsc --noEmit` will work again (Next.js generates `PageProps`/`LayoutProps` there).

200/200 web tests pass, `tsc --noEmit` clean, `next build` clean, eslint clean except the one pre-existing unrelated Sidebar `aria-disabled` warning (not touched). Not yet committed/merged — no branch/worktree used for this fix, done directly (small, bounded, test-verified at every step).

**Next:** decide S4 scope (CSV import backend, Revolut format) — or mark Story 2+3 Done in Jira first (still outstanding from before).

**S4 scoped via brainstorming (bounded path — refinement of the already-approved Epic 11 spec, no new spec file), then built via `subagent-driven-development` in an isolated worktree, first story to use it since the S2+3 sequencing fix.** Design covered in chat: `finance-api/src/transactions/csv/revolut-parser.ts` (pure Revolut CSV parser, new `csv-parse@7.0.2` exact-pinned dependency), `import-hash.ts` (dedupe fingerprint, brief §11 rule 3), `TransactionsService.previewImport`/`commitImport`/`exportCsv`, three new routes (`POST /transactions/import/preview`, `POST /transactions/import/commit`, `GET /transactions/export`), `Transaction.importHash String? @@unique([accountId, importHash])` (Postgres NULL-distinct semantics keep manually-entered transactions from ever colliding). One real design fork resolved with the user before planning: no server-side import-session/batch table — the client resends the full preview row set verbatim at commit, re-validated server-side (chosen over adding new schema). A second: Revolut's `Fee` column (undocumented in the original spec) becomes its own imported expense row (category `"Fee"`) when nonzero, with its own independently-hashed dedupe fingerprint — user chose completeness over matching the spec's silence.

Plan: `docs/superpowers/plans/2026-09-17-epic11-story4-csv-import-backend.md`, 7 tasks, committed to git this time (unlike every prior Epic 8/11 plan doc, which stayed untracked — a standing hygiene gap flagged repeatedly since Story 8, fixed here since nothing blocked it).

**Task-level findings, all resolved same-session:** Task 5 (`commitImport`) — the plan's own code (a per-row loop of individual `tx.transaction.create()` calls) was not actually race-safe: a concurrent or retried import could hit the DB's own `@@unique([accountId, importHash])` constraint mid-transaction and abort the *whole* batch instead of skipping the one duplicate, the opposite of the spec's "race-safe" language, and the same loop shape risked Prisma's 5s transaction timeout on a real multi-hundred-row statement. Ruled and fixed: rewritten as a single `tx.transaction.createMany({ skipDuplicates: true })` — one round-trip, `ON CONFLICT DO NOTHING` at the DB level. The plan's own rollback test also didn't exercise `commitImport` at all (tested raw `prisma.$transaction` instead) — a second plan-mandated test gap, fixed with a real `commitImport`-level failure test. Task 7 (`exportCsv`) had the same class of gap: its "only includes the caller's own transactions" test never seeded competing data for another user, so it passed even if the ownership filter were silently dropped — brief §12's negative-path rule tested only in appearance. Fixed with real seeded cross-user data plus a new multi-account coverage test.

**Final whole-branch review (opus) caught a real Critical bug no task-level reviewer could see, by actually running the shipped parser against realistic input:** `revolut-parser.ts`'s Fee-zero check compared the raw CSV string against the literal `'0'` — but a real Revolut export formats every numeric column to 2 decimals, so a zero fee arrives as `"0.00"`, which is `!== '0'`. Every zero-fee row (i.e. almost every row in a real statement) was silently spawning a bogus `-0.00` "Fee" expense transaction, each permanently deduped via its own real `importHash` on commit — a guaranteed-wrong result on the exact input format this story exists to consume, caught only because the reviewer ran the actual parser rather than trusting the Task 3 tests (which only covered the literal spellings `'0'` and `''`). Fixed: `Number(fee) !== 0`. Same review also caught 3 Important cross-task issues: a commit JSON body could exceed Express's silent 100kb default limit (~600 rows) well before the parser's 5000-row cap, so a real statement could preview but 413 at commit — fixed by raising the global JSON limit to 5mb via NestJS's `useBodyParser` API, independently verified (by both the fix's implementer and the re-reviewer) against the actually-installed `@nestjs/platform-express` source that the earlier default parser doesn't shadow it; the controller imported `memoryStorage` from `multer`, a transitive-only, undeclared dependency, for a default multer already provides on its own — fixed by dropping the import (verified against multer's own installed source); `commitImport`'s response was accidentally camelCase (`skippedDuplicates`), violating this project's binding snake_case-response convention one line away from `previewImport`'s correctly-snake_case response in the same file — fixed. One more finding folded into the same fix wave specifically because it can never be fixed retroactively once real user data exists: `computeImportHash`'s raw `"|"`-joined hash input was a latent collision surface for a `Description` containing a literal pipe — hardened to `JSON.stringify(parts)` before any real import could freeze the old format into stored data.

One residual finding from the fix-wave re-review adjudicated directly (no second fix wave — none allowed post-final-review): the added `Fee === Amount` collision test used fixture values (`"-1.00"`/`"1.00"`) that differ in the amount itself, making it mutation-inert as an end-to-end proof of the hash discriminator — parked as a test-naming weakness, not a real gap, since the discriminator mechanism itself is genuinely proven by `import-hash.spec.ts`'s actual equal-value collision test.

19 parked Minor findings recorded in the (now-deleted, git-history-is-the-record) SDD ledger, triaged by the final reviewer and left deferred — notably: no shared type ties `previewImport`'s response shape to `ImportCommitDto`'s input shape (nothing type-checks the round trip today, flagged as the highest-value follow-up for whichever story next touches this code), dedupe is scoped per-account so importing the same statement into two different Saldovio accounts double-counts with no warning (schema-consistent, worth a Story 5 UI note), commit amount travels the wire as a JSON number rather than a decimal string (consistent with `POST /transactions`'s existing convention, but Story 5 will be the second place this gets `Number()`'d client-side), and no CSV-formula-injection guarding on export (a category starting with `=`/`+`/`-`/`@` could be Excel-interpreted as a formula — out of scope per the original spec, worth a named backlog item since this is finance data people will open in Excel).

103/103 finance-api tests pass (up from 56 pre-story), `tsc --noEmit` clean (one confirmed pre-existing unrelated `test/app.e2e-spec.ts` error, predates this branch), `oxlint` clean. Merged to `main` locally (fast-forward, `832eb0d..399c727`), re-verified green on the merged checkout after `npx prisma generate` + `npm install` (same "worktree's generated client/new deps don't propagate to main automatically" step every dependency-adding story has needed since Story 8/Epic 11 S1). Worktree and branch cleaned up.

**Roadmap stage 7 (Epic 11) status: S1-S4 of 5 done** (schema+window, Settings UI, Simulator wiring, CSV import backend). S5 (CSV import UI + export/delete UI, though the export/delete *backend* work landed partly ahead of schedule — `GET /transactions/export` shipped in S4's Task 7 rather than S5) remains — the last unbuilt piece of Epic 11, and of MVP roadmap stage 7 overall.

**Next:** decide S5 scope (CSV import UI in the Settings Data card modal — preview table with per-row checkboxes, duplicate/error flags — plus wiring the already-built export/delete-account backends into the UI). Remember the parked "no shared preview↔commit wire type" recommendation when starting it.
